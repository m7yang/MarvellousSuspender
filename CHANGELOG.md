# Changelog

All notable changes to The Marvellous Suspender are tracked in this file.

Format inspired by [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).
The project adheres (pragmatically) to [Semantic Versioning](https://semver.org/).

Entries under "Unreleased" live on a feature branch until merged into `master`.

## [Unreleased]

### Fixed
- **Frozen suspended tabs logged a spurious "Failed to initialise tab … timeout" and were counted as init failures during a busy cold start** (`gsTabCheckManager.js`): when Chrome freezes a backgrounded suspended tab (MV3 tab freezing, common while dozens of tabs restore at once), `checkSuspendedTab()`'s `getSuspendInfo`/`initTab` `chrome.tabs.sendMessage()` calls get no answer and hang until the queue's 60s job timeout, which then emits a `[W] Failed to initialise tab … timeout` and resolves the check as failed — dragging the startup tally down (e.g. `40 / 46 initialised successfully`) even though every such tab is a perfectly valid suspended placeholder whose title and favicon were already applied before it froze. `checkSuspendedTab()` now resolves `STATUS_SUSPENDED` immediately when `tab.frozen` instead of running the message round trip: a frozen page can't answer it, and it can't establish its own title/favicon until it thaws, so requeuing until then would only hold `performInitialisationTabChecks()`'s `Promise.all` pending (leaving `gsInitialisationMode` on) until the queue's ~5-minute requeue cap — ending in the very timeout warning and failed tally this change removes. The common case is a tab Chrome froze *after* it finished rendering, so its title and `data:` favicon are already in place (`ensureSuspendedTabTitleAndFaviconSet()` — logged distinctly); a tab frozen before its `initialiseSuspendedTab()` work ran is still accepted, and reopens on the default title/favicon until it is next focused (its own responsiveness check) or, for the favicon, the #474 backstop repairs it. The startup resuspend requeue now also passes `refetchTab: true` (matching the equivalent requeue in the missing-view branch) so the frozen check, and the checks after it, read the tab's state *after* the resuspend reload rather than a stale pre-reload snapshot. The blocked-`file://` handling (unsuspend a suspended `file://` tab when file-URL access is off) was moved ahead of the frozen shortcut so a frozen blocked-file tab is still navigated back to its original URL rather than accepted as healthy — that path only navigates the tab, it never messages the page. The unsuspended-tab path (`checkNormalTab()`) is deliberately left unchanged.
- **Scheduled Drive auto-backup: a transient upload failure now retries with backoff instead of silently waiting for the next scheduled tick** (`gsBackup.js`): a one-off Drive failure (network blip, 5xx, 429) in the periodic `ALARM_NAME` auto-backup was only logged and dropped — `performBackup()`'s `catch` acted on `TMS_DRIVE_AUTH_MISSING`/`TMS_DOWNLOADS_PERMISSION_MISSING` but did nothing for anything else, so the next attempt was the next scheduled tick (up to 24h away on a daily interval). It now reuses the same recovery path the `onSuspend` emergency backup already had: on a non-auth Drive failure it queues `tmsPendingDriveBackup` and arms `RETRY_ALARM_NAME`, letting `retryPendingDriveBackup()` retry with the existing `[0.5, 2, 10]`-minute backoff and give up cleanly after the cap. `TMS_DRIVE_AUTH_MISSING` still short-circuits to the "Drive disconnected" flag as before (a retry can't fix a missing grant). The `catch`'s log line also now records `e.message`/`e.stack` instead of an Error that JSON-serialised to a useless `{}` in the debug report.

### Changed
- **`gsUtils.highlight()` no longer leaks a console format string into the log buffer** (`gsUtils.js`): it called `log('highlight: %s %c%s', 'color:red', …)`, but `log()`'s `console.log` branch is gated behind `gsUtils.debugInfo` (which nothing ever enables) and the only live sink — the captured log buffer — does no printf-style `%s`/`%c` substitution, so every buffered `highlight` line carried a literal `highlight: %s %c%s` label plus a stray `color:red` argument. Now logs under a plain `highlight` label; diagnostic reports read `highlight <tabId> getDebugInfo … <url>` instead.

## [9.0.3] — 2026-09-01

### Added
- **Suspend/unsuspend all tabs not in a group** (`tgs.js`, `background.js`, `shortcuts.js`, `manifest.json`, all 18 `messages.json` locales, #133): the counterpart to the tab-group actions, for the loose tabs left outside every group. Items in both context menus plus two bindable shortcuts, scoped to the window they were triggered from. Same suspend queue and `forceLevel` as the group actions, so whitelist, pinned, audible, app-window and form-input protections still apply.
- (follow-up) `src/CHANGELOG.md`'s symlink to the repo-root `CHANGELOG.md` (kept only for local "Load unpacked" testing) is removed, along with `Gruntfile.js`'s dedicated `copy:changelog` task that dereferenced it into packaged builds -- nothing reads `CHANGELOG.md` from inside the extension any more now that `gsChangelog.js` points at `CHANGELOG_USER.md` instead, so both were dead weight.
- **A short, user-facing "what's new" file, separate from this one** (`src/CHANGELOG_USER.md`, `gsChangelog.js`): this file has become a detailed engineering log -- real for anyone reading pull requests, but not what the "What's new" modal (added earlier this cycle) should be rendering at someone who just updated the extension. `CHANGELOG_USER.md` is a real file inside `src/` (not a symlink like this one's own local-testing copy), wiped and rewritten from scratch at every release with only the handful of changes an actual user cares about, plus links back here and to the relevant blog posts for anyone who wants the full technical detail. `gsChangelog.js` now points at it instead of this file; the parsing/rendering logic needed no changes at all, since both files share the same `## [version]` / `### category` / `- bullet` structure.
- **Separate auto-suspend timeout for battery power** (`gsStorage.js`, `options.html`, `options.js`, `style.css`, `tgs.js`, `gsUtils.js`, `gsTabSuspendManager.js`, `background.js`, all 18 `messages.json` locales, #252): new "Suspend tabs on battery power after" select shown side by side with the existing AC timeout (`gsTimeToSuspendOnBattery`, defaults to "Same as when plugged in", empty string, no override). When set to a specific value and the computer is currently unplugged, `resetAutoSuspendTimerForTab()` in `tgs.js` uses it instead of the normal timeout, lets tabs suspend more aggressively on battery without affecting the AC timeout, reusing the existing `navigator.getBattery()`/`tgs.isCharging()` plumbing already in place for the "never suspend while charging" option. The `battery.onchargingchange` listener in `background.js` now also resets all tabs' timers on a charge-state flip whenever a battery-specific timeout is set (previously it only did this for the "never suspend while charging" option), `gsUtils.js`'s per-tab settings-change handler resets a tab's timer when this option itself changes while on battery, and `checkTabEligibilityForSuspension()` in `gsTabSuspendManager.js` uses the same effective-timeout logic so a battery timeout still works even if the AC timeout is set to "Never". `isCharging()` can return `undefined` (not `false`) before the battery API's first read resolves or when it's unavailable in this MV3 service worker; every check here treats that as "unknown, don't override" rather than "on battery". `resetAutoSuspendTimerForAllTabs()` now clears only the per-tab suspension alarms instead of `chrome.alarms.clearAll()`, so a charge-state flip no longer wipes the unrelated auto-backup/retry/news-feed alarms.
- **Debug page: live power-source indicator** (`debug.html`, `debug.js`, `img/icons.svg`): new "power source" row in Controls shows a plug/battery icon and "AC power"/"on battery" text for the same `navigator.getBattery()` read the new battery-timeout option (above) and the existing "never suspend while charging" option rely on, updating live on `onchargingchange`, a quick way to confirm what the extension currently sees without guessing from behaviour alone. An equivalent indicator was first tried directly in Options next to the battery-timeout select, but ended up fighting that page's `.formRow > .topLabel { flex-basis: 100% }` layout rule for no real benefit; the debug page's existing icon+label+live-value row pattern (already used for the news feed status) fit it far better without any CSS wrestling.
- **"Always reopen suspended tabs scrolled to the top" option** (`gsStorage.js`, `options.html`, `options.js`, `tgs.js`, `contentscript.js`, all 18 `messages.json` locales, #308): new checkbox in Options → Suspended tabs (`gsDontRestoreScrollPos`, off by default) skips restoring the saved scroll position on unsuspend; the tab reopens at the top of the page instead of wherever it was scrolled to when suspended, with a tooltip explaining it applies to both automatic and manual suspension and overrides `#section` link anchors. Grouped with the other unsuspend-time behaviours (e.g. "unsuspend on focus") rather than the auto-suspend-trigger options above, since it's about what happens on reopen, not what triggers suspension. Implemented at `unsuspendTab()` in `tgs.js`, which sends an explicit `'top'` sentinel to `contentscript.js` when the option is on, forcing the scroll position to 0 even when the URL has a `#fragment` that would otherwise let the browser's native anchor-scroll override it.
- **Opt-in: reload also unsuspends background tabs** (`gsStorage.js`, `suspended.js`, `options.html`, `options.js`, all 18 `messages.json` locales, #158): the "reload to unsuspend" detection only ever counted a reload as an explicit user request when the reloaded tab was the currently focused one; reloading a suspended tab in the background (via Ctrl+R on a multi-tab selection, or right-click → Reload on an inactive tab) silently did nothing, since the tab's own `beforeunload` handler skipped recording the trigger for anything but the focused tab. New "Reload also unsuspends tabs in the background" checkbox in Options → General lifts that focus check when enabled (off by default, so nothing changes for existing users); doesn't touch Chrome's or TMS's own background-tab discarding logic, only whether a reload on such a tab gets recognized afterwards. Ships with an explanatory tooltip (all 18 locales) since the behaviour change is easy to miss from the label alone.
- **Drive auto-backup: PKCE + refresh_token replaces the implicit-flow fallback on Brave/Vivaldi** (`gsBackup.js`, `manifest.json`, #437): on browsers where `chrome.identity.getAuthToken()` doesn't work (Brave, Vivaldi), Drive auto-backup fell back to `launchWebAuthFlow()`'s implicit flow — a short-lived (~1h) access token, silently renewed via `launchWebAuthFlow({ prompt: 'none' })`. That renewal depends on the browser's ambient Google session for that tab, which isn't reliable on Brave/Vivaldi once the token expires, and was the reported cause of Drive accounts flipping to "disconnected" after just 1-2 automatic backups. The fallback now uses authorization-code + PKCE instead: a one-time interactive consent mints a long-lived `refresh_token` (`chrome.storage.local`), and every renewal after that is a direct POST to `oauth2.googleapis.com/token` — no tab, no cookies, no dependency on ambient browser session state. Reuses the existing "Web application" OAuth client (#420) rather than registering a new one, since Google's "Desktop app" client type (verified live against the authorize endpoint) rejects `chrome.identity.getRedirectURL()`'s `https://<ext-id>.chromiumapp.org/` redirect with `redirect_uri_mismatch` — that client type only accepts loopback/`urn:ietf` redirects. `manifest.json`'s `connect-src` gained `https://oauth2.googleapis.com` (previously only `www.googleapis.com`/`accounts.google.com` were allowed), without which the token-exchange `fetch()` failed silently. `src/js/gsOauthSecrets.js` is now a *committed* placeholder (`PKCE_CLIENT_SECRET = 'REPLACE_ME'`) rather than a gitignored file the static `import` could fail to find — a Codex review round caught that a missing import here failed the whole module and stopped the service worker from registering at all, for every user, not just ones touching Drive backup. A second review round then caught that the first fix (a lazy runtime `import()` instead) traded that bug for a worse one: MV3 service workers don't support dynamic `import()`, so the automatic, non-interactive token refresh — which runs in the service worker, unlike an interactive "Connect" click from `backup.html`'s own page context — would have silently failed every time, masked during manual testing because the interactive path never hit it. The real secret now lives only in a new gitignored `src/js/gsOauthSecrets.local.js`, and Grunt's build pipeline (`checkOauthSecrets` + a new `string-replace:oauthSecret` task) substitutes it into the *packaged build's own copy* of `gsOauthSecrets.js`, leaving the tracked placeholder untouched. **Migration note for existing Brave/Vivaldi users already connected via the old implicit flow**: no prior release could have stored a `refresh_token` for them, so once their current short-lived session expires (within about an hour of updating), the next automatic backup attempt fails and the extension flags the existing "Drive disconnected" badge — expected, unavoidable given Google requires interactive consent to mint a refresh token, and fixed with a single manual "Connect" click in Options → Backup, same as any other reconnect. Everyone reconnecting from that point on gets the new, more reliable renewal path with no further action needed. **Known, accepted limitation — the client secret ships inside the packaged extension**: a third review round pointed out that a "Web application" OAuth client's secret (the type used here) is, by Google's own classification, meant to stay confidential server-side — unlike "Chrome app"/"Desktop app"/mobile client types, which Google explicitly documents as *not* needing real secret confidentiality (installed apps can't keep a secret truly secret, so Google doesn't ask them to). This project's client can't use one of those exempted types instead: a "Chrome app" client only works with `chrome.identity.getAuthToken()` — the very API failing on Brave/Vivaldi that this whole fix exists to work around — and a "Desktop app" client rejects `launchWebAuthFlow()`'s `chromiumapp.org` redirect outright (verified live against Google's authorize endpoint). Since the extension package (CRX/ZIP) is trivially unpacked by anyone, this secret should be treated as effectively public once shipped. Accepted as a known limitation rather than standing up a separate backend service purely to broker this one exchange: a leaked secret only lets a third party register something that authenticates to Google *as this app* (e.g. a phishing consent screen impersonating it) — it does not expose any existing TMS user's Drive data, refresh token, or access token, since those never leave that user's own browser storage. Revisit if this ever needs hardening further. A fourth review round then caught two more, both in error-handling around the refresh token itself: `refreshAccessToken()` discarded the stored `refresh_token` on *any* 400/401 from the token endpoint, but a 400/401 doesn't only mean the token itself is dead (`invalid_grant`) — it can also mean a client-level failure like `invalid_client` (e.g. while the OAuth secret is mid-rotation) that has nothing to do with that specific token's validity; discarding it there forces every affected user through a fresh consent screen once the client itself is fixed, for a token that was never actually the problem. `exchangeTokenEndpoint()` now exposes the parsed `error` field on the thrown error, and only `invalid_grant` triggers the discard. Separately, `revokeAuthToken()`'s Drive "Disconnect" button ignored the revoke `fetch()`'s response entirely and cleared the local `refresh_token` regardless of outcome — if the revoke call failed (blocked network, a transient 5xx), the long-lived grant stayed fully active server-side with no local token left to ever retry revoking it, silently leaving the account "disconnected" in the extension's UI while still authorized on Google's side. `revokeAuthToken()` now only clears local state when the revoke call actually succeeds (or Google reports the token already invalid via a 400, which is equivalent to done); on any other failure it throws instead, and `backup.js`'s disconnect handler surfaces that as a status message instead of silently reporting success — reusing the existing `js_options_backup_drive_auth_error` string (already translated in all 18 locales via Crowdin) rather than a new dedicated key, since a new key was first added only to en/it and a later review round caught it would render as an empty string in every other locale until the next Crowdin sync. A fifth review round then flagged that `checkOauthSecrets` now makes `grunt`/`npm run build` fail hard for anyone without the maintainer's private OAuth secret, even for a contributor who just wants a local zip/crx build with no interest in Drive at all. Deliberately left as-is rather than downgrading to a warning: `crx:private` a few steps later in the same task list already has the identical shape of requirement — a gitignored `key.pem` signing key, equally maintainer-only — so this doesn't introduce a new class of "can't build without a project secret" problem, only a second instance of an existing, accepted one specific to this repo's release tooling. It doesn't affect testing the PKCE flow itself, either: "Load unpacked" straight from `src/` never invokes Grunt, and anyone can still create their own `gsOauthSecrets.local.js` locally to test that path (see `gsOauthSecrets.example.js`) without ever running `grunt`.
- **"What's new" changelog modal on the Settings page** (`gsChangelog.js`, `gsStorage.js`, `options.html`, `options.js`, `style.css`, `Gruntfile.js`, all 18 `messages.json` locales): after an update, opening Settings now shows a one-time modal with just the current version's entries from this file (not the whole history), parsed client-side with a small regex-based markdown-to-DOM renderer (no `innerHTML`, so the source can't inject markup). Shown once per version (`gsLastSeenChangelogVersion` in `chrome.storage.local`), skipped entirely on a brand-new install. `CHANGELOG.md` lives at the repo root as usual; a `src/CHANGELOG.md` symlink keeps it available for local "Load unpacked" testing, and a dedicated Grunt copy step ships a real (dereferenced) copy inside the packaged extension.
- **Debug page: "reset changelog seen flag" action** (`debug.html`, `debug.js`): clears `gsLastSeenChangelogVersion` so the new changelog modal above can be re-tested from an unpacked/dev install without bumping the manifest version each time; a normal `Load unpacked` reinstall always sets the "first time" flag, which intentionally skips the modal, so there was no other way to re-trigger it locally.
- **Debug page: "repair favicons now" action** (`debug.html`, `debug.js`, `background.js`, `gsSession.js`, `suspended.js`, #449): "Clear favicon cache" only empties the cache; it doesn't force a re-check of currently open suspended tabs, so anyone needing an on-demand repair (session restore after a crash, "continue where you left off", or pre-9.0.2 installs without #397's startup-sentinel fallback) had no option short of restarting the browser again or unsuspending everything. The new action reuses `gsSession.performTabChecks()` (already decoupled from `runStartupChecks()`'s crash-recovery/version-branching logic) against the currently open tabs on demand, routed through the service worker like the existing `setCaptureLogs`/`clearLogs` actions since `gsTabCheckManager`'s check queue only exists there. Surfaced a pre-existing bug while wiring the response up: `chrome.runtime.sendMessage` with no `tabId` broadcasts to every extension page, not just the service worker, and every open `suspended.html`'s message listener was resolving `true` for actions it didn't even handle (an async function listener always returns a Promise regardless of its internal logic), racing against, and sometimes beating, the real response and turning the reported repair counts into `undefined`/`undefined`. `suspended.js` now dispatches synchronously against a known-actions list first, returning a real `false` for anything it doesn't own so it stops shadowing other listeners' responses.
- **Suspend/unsuspend all tabs in a tab group** (`tgs.js`, `background.js`, `manifest.json`, all 18 `messages.json` locales, #133): new context menu items "Suspend/Unsuspend all tabs in this group", both in the page context menu and in the tab-strip (right-click on tab) menu, plus two bindable keyboard shortcuts (`2c-suspend-tab-group`, `2d-unsuspend-tab-group`). Both act on every tab sharing the clicked/active tab's `groupId` and route through the standard suspend queue, so whitelist, form-input, and audible-tab protections apply as usual; on an ungrouped tab they are a no-op. The four new strings were translated by hand for all 16 non-English/Italian locales (reusing each locale's existing suspend/unsuspend and "all tabs in this window" phrasing) rather than left for the next Crowdin sync, so `npm run check-locales` stays clean.
- **"Never suspend app windows" option** (`gsStorage.js`, `gsUtils.js`, `gsTabSuspendManager.js`, `options.html`, `options.js`, all 18 `messages.json` locales, #154): new checkbox in Options → Auto-suspend (`gsDontSuspendAppWindows`, on by default like the sibling pinned/audio/active-tab protections), right after "never suspend pinned tabs". Covers both "Create Shortcut → Open as window" and an installed PWA ("Install <site>" from the address bar) — both open in a `chrome.windows` window of `type: 'app'`, not `'normal'`. Deliberately scoped to `'app'` only, not `'popup'`: the latter also catches a site's own transient `window.open()` popups, not the "app-like tool I keep open" case this option is for. New `gsUtils.isProtectedAppWindowTab()` follows the existing `isProtectedPinnedTab()`/`isProtectedAudibleTab()` pattern, wired into `checkTabEligibilityForSuspension()` at the same `forceLevel >= 2` check as its siblings (a manual "suspend this tab now" still bypasses it, only automatic suspension respects it) and into `performPostSaveUpdates()`'s existing "unsuspend already-protected tabs when this setting flips on" logic. The new string was translated by hand for all 16 non-English/Italian locales (reusing each locale's existing "never suspend pinned tabs" phrasing) rather than left for the next Crowdin sync, so `npm run check-locales` stays clean. A Codex review round caught that disabling the option again didn't re-arm the timer of an app-window tab whose auto-suspend alarm had already fired and been rejected while the option was on -- `performPostSaveUpdates()`'s timer-reset predicate had a case for pinned/audible but none for this new setting. New `gsUtils.isTabInAppWindow()` (the window-type check on its own, without the setting gate `isProtectedAppWindowTab()` also does) lets that predicate ask "is this tab in an app window" independently of the setting's now-already-flipped value, matching the existing pinned/audible pattern. Manual testing then caught that `calculateTabStatus()` in `tgs.js` had no dedicated branch for this protection either (unlike pinned/audible/active, each with their own `STATUS_*` label) -- an app-window tab whose content script hadn't reported in yet showed as `unknown` in the debug page's tab list instead of a status that actually explains why it's protected. New `gsUtils.STATUS_APP_WINDOW` ('appWindow') and a matching branch close that gap. A further Codex review round then caught that `popup.js`'s `setStatus()` had no branch for the new status either -- opening the toolbar popup on a protected app-window tab fell through to its final `else` (a console warning) and left `statusDetail` blank, an empty status line instead of an explanation. New `js_popup_app_window` string (all 18 locales) and a matching `else if`, same pattern as the existing `js_popup_pinned` branch right above it.

### Changed
- **Drive PKCE token exchange moved server-side to `tms-oauth-proxy`, removing the OAuth client secret from the shipped package** (`gsBackup.js`, `manifest.json`, `Gruntfile.js`, #437 follow-up): the initial writeup of the PKCE fallback (see the entry above) flagged shipping the "Web application" client secret inside the extension as a known, accepted limitation, on the reasoning that a leaked secret's only real use was impersonating the app for a phishing consent screen. Re-examined that: Google's OAuth policy expects a "Web application" client's secret to stay confidential server-side, and a client that ships it publicly risks the client itself getting flagged or suspended by Google, which would break Drive backup for every user at once, not just a narrow phishing blast-radius. Fixed properly instead of documented around: `exchangeTokenEndpoint()` in `gsBackup.js` now POSTs to a new Cloudflare Worker (`tms-oauth-proxy`, a separate private repo under the Marvellous Codeworks org) instead of directly to `oauth2.googleapis.com/token`; the worker holds the client secret as an encrypted Cloudflare secret and forwards the exchange, so the extension itself never has it. `PKCE_CLIENT_ID` (public, harmless to embed) stays in `gsBackup.js` for the initial authorize-URL redirect, which never needed the secret anyway. Removed entirely: `src/js/gsOauthSecrets.js`/`.example.js`/`.local.js`, the `checkOauthSecrets` build guard, and the `string-replace:oauthSecret` Grunt task — `npm run build` no longer needs a maintainer-only secret file to produce a working build. `manifest.json`'s `connect-src` swapped `https://oauth2.googleapis.com` for the worker's `https://tms-oauth-proxy.gioxx.workers.dev`. The exposed client secret was rotated in Google Cloud Console as part of this change, since the old one had already been shared during testing.
- **Options → Suspended tabs: grouped "don't restore scroll position" next to "keep YouTube playback position"** (`options.html`): both control what state gets restored on unsuspend, but the scroll-position checkbox sat several rows away, next to "reload also unsuspends in the background" and "discard after suspend" instead. Moved directly under the YouTube-timestamp checkbox so the two page-state-restoration options read together.
- **Debug page: Controls section redesigned to cut wasted vertical space** (`debug.html`, `debug.css`, `debug.js`): the 7 option boxes were all forced to the height of the single tallest one via `equalizeCardHeights()` (mostly `captureLogs`'s 3-line description), even the ones with a single line of text. The two behavioural toggles with real explanations (`captureLogs`, `discardInPlaceOfSuspend`) keep the full 2-column card layout; the other 5 (claim suspended tabs, favicon cache, changelog modal reset, news feed, backup device identity) moved into a compact single-line row list. `equalizeCardHeights()` removed as dead code; the 2-card grid stretches evenly on its own via native CSS grid behaviour.
- **Issue reporting switched from a free-form template to structured GitHub issue forms** (`.github/ISSUE_TEMPLATE/`, #479): `bug-report.yml`/`feature-request.yml` require version, browser, OS, install source, and repro steps up front instead of relying on reporters to fill in a markdown template by hand; `config.yml` disables blank issues and links the KB troubleshooting page, the diagnostic-report guide, and Discussions. Bug reports auto-label "to be investigated" (not "bug", reserved for confirmed reports per the triage taxonomy); feature requests auto-label "enhancement" + "to be evaluated". The old `.github/issue_template.md` is removed.
- **Removed two dead GitHub Actions workflows inherited from the original TheGreatSuspender repo** (`.github/workflows/`, #479, #480): `IssueAssignment.yml` auto-commented "the project will soon be closed" on every new issue -- contradicts active 9.0.x development and undermines the new issue forms above. `stale.yml`, last touched in 2018, configured the deprecated probot Stale app (not installed here, no "stale" label exists) and referenced exempt labels that no longer exist in the current taxonomy; had it ever run, it would have auto-closed confirmed bugs and in-progress work, contradicting the triage process's requirement that a human review and draft every close.

### Fixed
- **Suspended tabs kept showing the TMS extension favicon after a cold browser start on Chromium forks (Brave, Vivaldi, Dia), needing a manual Tab Health reload or the #449 "Repair favicons now" action every restart** (`background.js`, `gsSession.js`, `gsMascot.js`, `gsFavicon.js`, #474; #397/#260 lineage): the automatic favicon pass runs inside `gsSession.runStartupChecks()` → `performTabChecks()`, driven by `chrome.runtime.onStartup` plus the `gsStartupOnceRun` `chrome.storage.session` sentinel fallback. That sentinel only records that startup was *attempted* — if the MV3 service worker is recycled mid-run (forks recycle harder during the busy cold-start window) or suspended tabs aren't loaded yet when the single pass runs, the pass is lost and the sentinel then suppresses any retry for the rest of the browser session. The #474 reporter saw it miss tabs on plain Chrome 151 too. A dedicated, `onStartup`-independent backstop now owns its own `chrome.storage.session` state, separate from `gsStartupOnceRun`. A *repair cycle* is one `performTabChecks()` pass, a short settle, then a count of suspended tabs still on a *repairable* favicon (missing, or the extension icon — a valid-but-generic favicon is left to Tab Health). `runStartupChecks()`'s existing pass is the session's first cycle; while the session isn't finalised, a one-shot `chrome.alarms` alarm (`tms-favicon-repair-backstop`, ~30s) and the first `chrome.tabs.onActivated` on a suspended tab each run another. Cycles are serialised through a single in-flight promise (a trigger arriving mid-cycle awaits it rather than starting a second, concurrent `performTabChecks()`), with a 15s minimum gap so a tab-switch storm can't spin them. Bookkeeping is in `chrome.storage.session` so it survives a mid-cycle worker recycle: an *attempts* counter is written **before** each pass and re-checked against a cap of 3 at entry (so repeated recycling can't relaunch the pass forever) and is reset to 0 by any clean count; a *clean-streak* counter only finalises the session — set the done flag, clear the alarm — after **two consecutive** clean cycles, so a clean snapshot taken before a long "continue where you left off" tab set has finished restoring doesn't disarm the backstop while tabs are still coming back on the extension icon. A cycle that finds broken tabs, or whose pass throws, resets the streak and re-arms the alarm itself rather than trusting a future worker spawn to; at the cap it stands down and leaves the #449 manual action as the escape hatch. The split-incognito worker (shared `chrome.storage.session`, but `chrome.tabs.query()` sees only incognito tabs) runs the pass but touches none of that shared state — the regular worker owns the backstop. Once finalised every trigger short-circuits on one `chrome.storage.session` read; never a periodic alarm, so no thrash on the ~30s worker idle-recycle. The "is this the extension icon" check — and `gsFavicon`'s repair path, and `health.js`'s Tab Health scan — matches **both** the current and legacy-mascot favicon URL via `gsMascot.resolveBothUrls()`, regardless of the `gsLegacyMascot` setting; an early build matched only the current one, so with legacy mascot enabled the count read 0 and the backstop stood down without repairing anything. Verified live on Brave 152 with a real suspended-tab session: after a cold restart the cycles ran, re-checked, and finalised (`2 consecutive clean passes`) with every favicon repaired and no manual step; the backstop alarm then stood down. An earlier build was also confirmed on a relaunch where Brave fired no `onStartup`/`activate`/sentinel at all, recovered by the `onActivated` trigger.
- **Tab Health under-counted "tabs showing the extension icon" for anyone with the legacy mascot enabled** (`health.js`): `scanTab()` matched only `img/ic_suspendy_16x16.webp`, so a suspended tab showing `img/legacy/ic_suspendy_16x16.webp` was neither flagged as the extension icon nor (being a real WEBP) as invalid — it fell through both branches and was left out of both the `favExtension` and `favDefault` buckets entirely, so the report read 0 while tabs were visibly broken. Matches both URLs now, same as the #474 backstop's own check.
- **Options → Auto-suspend: the "suspend on battery after" select's wrapper was an unnecessary `formRow`, unlike its sibling column and the equivalent Language/Theme pair above it** (`options.html`): triggered `style.css`'s `.formRow > .topLabel { flex-basis: 100% }` rule (already flagged as a footgun for this exact pair in an earlier entry above) inside the nested flex row, throwing off the vertical spacing of the whole pair relative to its section title -- visible side by side with the still-correct Language/Theme pair, spotted while testing the app-window option above. Changed to a plain `<div>`, matching every other paired-select column on this page.
- **Health and About pages: intro text styled inconsistently with the rest of the extension's internal pages** (`health.html`, `about.html`): both rendered a `.pageTitle` intro at full body size/color instead of the `<p class="sectionDesc">` pattern (smaller, muted-color) used consistently on `history.html`, `backup.html`, and `news.html` -- `health.html`'s used a plain `<div class="bottomMargin">`, `about.html`'s "Support development" intro used an unclassed `<p>`. Both switched to match; `shortcuts.html`'s `.heading-note` under its own `.pageTitle` is a deliberately distinct, single-use italic callout component, not this same inconsistency.
- **Blinking white background when switching between suspended tabs** (`suspended.html`, `css/critical.css`, #168): `html` had no background of its own until `suspended.css`/`style.css` finished loading (both `@import` `tokens.css`, a second sequential request each), so the browser's own default white showed for that gap -- a bright flash switching tabs in a dark room, reported as far back as v7.1.6.2. A manifest `style-src 'self'` CSP rules out an inline `<style>` block (the obvious fix); instead, a new standalone `critical.css` with no `@import` of its own loads first and sets `html`'s background from the OS `prefers-color-scheme` immediately, using the same `oklch` values as `tokens.css`'s `--background` token so there's nothing to visibly hand off once the real stylesheets land. A Codex review round caught that this OS-only guess is wrong whenever `gsTheme` is an explicit override rather than `'system'` (an explicit dark theme on a light OS still flashed light, and vice versa) -- a new classic, render-blocking `js/criticalTheme.js` now runs before `critical.css` is evaluated and applies a cached explicit theme to `html`, mirrored into `localStorage` by `gsUtils.js`'s `setPageTheme()` on every previous load; the OS-preference fallback still covers the very first suspend, before any cache exists. A further review round caught that this cache was written even for the `'system'` setting, though -- if the OS `prefers-color-scheme` changed later (e.g. a scheduled night theme), that now-stale cached class kept overriding the live media query in `critical.css`, recreating the flash for the default theme mode specifically. `setPageTheme()` now only caches an *explicit* dark/light override, and clears any stale cache when the setting is `'system'`, handing paint back to the media query immediately. **Known, accepted limitation**: a synced `THEME` change landing while a given suspended tab isn't currently loaded leaves that tab's cache stale until it's next reactivated -- MV3 service workers have no `localStorage` of their own to refresh it directly, and closing this fully would need a `document_start` content script, disproportionate for this narrow an edge case; self-corrects within one async cycle on that tab's next reactivation.
- **Suspended page: the reload hotkey link was too bright/conspicuous in dark mode** (`css/suspended.css`): reported by a user via email with a screenshot. The link inherited `style.css`'s generic accent-blue `a` color, the same used for real, actionable hyperlinks elsewhere in the extension UI -- too prominent on this deliberately quiet, Chrome-native-styled "sleeping" page. Toned down to a subdued gray, with the accent color kept on `:hover`/`:focus` only. A Codex review round caught that `--muted-foreground` (the first choice) composites to only ~3.6:1 against the light-theme background, under the 4.5:1 text-contrast threshold, for what's normal-sized body text and the only link shown when no reload shortcut is configured; switched to an opaque `oklch(0.42 0 0)` tuned to clear 4.5:1 on light, keeping `--muted-foreground` for dark (already ~4.8:1, unaffected).
- **Debug page: the live log view showed only a bare `HH:MM:SS`, indistinguishable from the same time on a previous day** (`debug.js`): the store rotates rather than clearing on its own, so yesterday's entries routinely sit alongside today's in the same view. `formatLocalTime()` now prefixes the local date whenever an entry isn't from today, leaving the common case (today's own session) unchanged.
- (follow-up, Codex review) `clearLogBuffer()`'s `clearedAt` cutoff write to `chrome.storage.local` was best-effort -- a failed write still let the clear proceed without it, silently reopening the exact cross-context race the cutoff exists to close (`gsUtils.js`). It's now a prerequisite: the clear fails (IndexedDB untouched) if the cutoff can't be persisted, matching `debug.js`'s existing "clear failed" handling.
- (follow-up, Codex review) `trimLogEntries()` ran its full `getAllKeysFromIndex()` scan unconditionally on every service worker init (`background.js` calls it there, not just from the periodic alarm), even when the store was already within its cap (`gsIndexedDb.js`). A cheap `db.count()` check now short-circuits that scan on an ordinary restart.
- (follow-up, Codex review) Removing `gsOauthSecrets.local.js` from `.gitignore` (see the OAuth-proxy entry above) didn't stop a maintainer's already-existing, untracked local copy from being packaged -- `Gruntfile.js`'s `copy:main` still globbed `src/**` unrestricted. Re-added the ignore entry and excluded the file from `copy:main` explicitly.
- **`unsuspendSelectedTabs()` unsuspended the highlighted tabs one at a time, sequentially awaited, unlike `unsuspendTabGroup()`'s already-shipped fire-and-forget pattern** (`tgs.js`, #461): no functional bug, just an inconsistency between two equivalent multi-tab unsuspend actions -- selecting several tabs and unsuspending them now fires every `unsuspendTab()` call at once instead of waiting for each one to finish before starting the next, matching how the group action already behaves.
- (follow-up) The log-buffer hint on the debug page now ends with a line explaining that "Download report"/"Copy report" can take a few seconds (longer with a lot of open tabs) and that the button's own "Working…" state means a second click isn't needed and won't do anything -- the disabled-while-busy fix just above made a slow report visibly in-progress, but nothing on the page explained *why* it might take a while in the first place.
- **The log-trim fix above only kicks in on its own 5-minute alarm schedule, so an install that had already grown well past the 10,000-entry cap (confirmed live at 12,721) stayed oversized for up to 5 minutes after every service worker restart until the alarm's first tick actually landed** (`background.js`): a report build against that oversized, still-growing store was correspondingly slower than expected right after this fix shipped, which looked like a regression in the immediately preceding `getAll()`-based report-fetch change rather than what it actually was -- a separate, larger store the alarm hadn't caught up to yet. One immediate `trimLogEntries()` call now runs alongside `syncLogTrimAlarm()` at service worker init, from the same single place, catching an oversized store up right away instead of waiting on the first periodic tick.
- **Repeatedly clicking "Copy report"/"Download report" while a report was still being built silently started another full, overlapping build each time -- for Download specifically, each click saved its own file, so a few impatient extra clicks (nothing on the page indicated it was working) turned into several downloaded files** (`debug.js`): `buildReport()` does a real, potentially slow pass -- a concurrency-capped sweep of every open tab plus, for the full download variant, reading and sorting the entire log-entries store (thousands of records on a long `captureLogs` session). Both buttons now disable themselves and show "Working…" for the duration, making a slow report visibly in-progress instead of silently doing nothing, and a second click physically impossible until the first finishes.
- **The log-entries trim alarm never actually fired, so the store kept growing well past its 10,000-entry cap** (`gsIndexedDb.js`): confirmed live via a report showing 12,721 stored entries. `chrome.alarms.create()` with only `periodInMinutes` (no `when`) fires for the first time `periodInMinutes` *after* the call, and calling `create()` again with the same name replaces the alarm outright, restarting that countdown -- `syncLogTrimAlarm()` ran unconditionally on every service worker init, and MV3 service workers restart routinely (well under 5 minutes apart under any real activity), so the alarm's countdown kept resetting before it ever reached zero. It now checks for an existing alarm first and only creates one if none exists; `chrome.alarms` persist independently of the service worker's own lifetime once actually created, so the schedule set on first creation keeps firing on its own regardless of how often the worker restarts afterward.
- **Moving the legacy chrome.storage.local key cleanup to run only from the service worker's own init didn't actually fix the broadcast-fanout risk it was meant to** (`background.js`): that fix bounded *who* calls `chrome.storage.local.remove()` on those orphaned keys, but `chrome.storage.onChanged` broadcasts a removed key's full `oldValue` to every context with a listener registered regardless of which context called `remove()` -- a profile with an already-accumulated multi-MB `gsLogBufferFull` would still deliver that same payload to every suspended tab on the one call that actually succeeds, no matter how few contexts attempt it. The cleanup is removed entirely instead: these keys are genuinely orphaned and harmless left in place, a far better trade than risking that broadcast during exactly the many-suspended-tabs scenario that caused the original crash.
- **Treating an `initTab` message timeout as terminal (see above) didn't stop the follow-up responsiveness check from still running a few seconds later, recreating the exact duplicate-work problem that fix was meant to prevent** (`tgs.js`): the timeout was caught and only logged, with nothing tracking that the send had actually failed -- the existing `token.cancelled` guard in front of `gsTabCheckManager.queueTabCheck()` only covers explicit cancellation (a tab discarded, navigated away, etc.), not this case. `checkSuspendedTab()` would then see a tab whose real `initTab()` might still genuinely be running in the receiving page and dispatch a second, fully duplicate one. A new `sendFailed` flag now also skips that follow-up check on any send failure, not just an explicit cancellation.
- **The 10s send-timeout added just above could itself trigger duplicate, fully concurrent `initTab()` runs in the same suspended page** (`tgs.js`): a timeout only ever rejects `_withTimeout()`'s own wrapper promise -- it can't actually cancel the underlying `chrome.tabs.sendMessage()` call or whatever real work (favicon decode, canvas, preview setup) the receiving page's `initTab()` had already started doing by the time it fired. Retrying after a timeout, as every other failure already did, fired a second `'initTab'` at the same page racing the first, and the same could repeat on every subsequent timeout -- up to the full retry budget's worth of fully duplicate, concurrent real work per tab, across every concurrency slot doing the same thing, recreating the exact kind of memory burst this whole limiter exists to prevent. A timeout is now treated as terminal rather than retried, distinct from a normal quick send failure (no receiver yet, page still loading its own script), which still legitimately benefits from a short retry.
- **`suspended.js`'s `'initTab'` message handler could hang the sender forever if it threw partway through, or if the receiving page's own thread genuinely froze** (`suspended.js`, `tgs.js`): `messageRequestListener()` called its async `handleMessageRequest()` without awaiting or catching it, so an unhandled rejection inside (e.g. `initTab()`'s own awaited storage/favicon calls failing) meant `sendResponse()` simply never ran for that message. `chrome.tabs.sendMessage()` has no timeout of its own -- the sender's promise never settles either way, permanently holding one of `tgs.js`'s five `initSuspendedTab` concurrency slots; enough of these and every future suspended tab stays queued indefinitely. `messageRequestListener()` now catches that rejection and calls `sendResponse()` regardless, and `sendInitTabMessageWithRetry()` in `tgs.js` additionally wraps each attempt in its own 10s timeout, closing the remaining gap a genuinely frozen (not throwing) receiving page would still leave.
- **A startup tab job that legitimately requeues several times (common under load, e.g. many tabs restored or reinitialised together) could stay in `gsTabCheckManager`'s queue for minutes with `gsInitialisationMode` stuck `true` the whole time, disabling popup/recovery behaviour** (`gsTabQueue.js`): the per-attempt timeout reset added to `requeueTab()` in an earlier fix (giving real progress a fresh `jobTimeout` window instead of accumulating elapsed time across requeues) left `MAX_REQUEUES` (100) as the *only* terminal bound, and 100 requeues at the normal several-second delay between them could run for far longer than any single `jobTimeout` was ever meant to represent -- roughly six minutes in the case Codex flagged, for a queue configured with a 60s `jobTimeout`. A separate overall deadline (`deadlineAt`, set once the first time a job is ever processed, five times the configured `jobTimeout` -- generous enough not to punish the legitimate multi-requeue case the per-attempt reset exists for, but still an actual ceiling) is now checked in `requeueTab()` alongside `MAX_REQUEUES`, independent of how many requeues it took to reach it.
- **The log-buffer coalescing fix above (refreshLogs() overlap) didn't actually resolve the crash it was aimed at -- the same OOM signature recurred at the same ~3.7-4GB ceiling regardless of tab count, and a heap snapshot (taken via chrome://inspect right before a subsequent crash) finally showed the real mechanism directly: dozens of near-duplicate ~6MB strings, each a full JSON-stringified copy of the persisted log buffer, retained simultaneously and hours out of date** (`gsUtils.js`, `gsIndexedDb.js`, `debug.js`, `background.js`, `gsStorage.js`): the previous design persisted the log buffer as one shared `chrome.storage.local` blob, written only by the service worker; every other context (every suspended tab included) sent its own pending entries there via a broadcast `chrome.runtime.sendMessage()`. The actual leak had nothing to do with that message path -- it came from `chrome.storage.onChanged`, which Chrome fires in *every* context with any listener registered for that storage area, delivering a full copy of whatever changed regardless of whether that context's own callback cared about the keys involved. `suspended.js` registers exactly such a listener (for an entirely unrelated setting), present in every one of dozens of suspended tabs sharing one renderer process (same-origin extension pages share a process) -- so every single write to the log buffer delivered a multi-MB oldValue/newValue copy to every suspended tab at once, none of it released once the callback finished ignoring it. Replaced the entire persistence layer with IndexedDB (`gsIndexedDb.js`'s new `DB_LOG_ENTRIES` store, one record per log entry via the store's usual keyPath/autoIncrement pattern, `DB_VERSION` bumped 3->4): IndexedDB writes have no equivalent cross-context broadcast, and giving each entry its own record means every context can now write directly without funneling through the service worker as a single designated writer -- concurrent writers from different contexts no longer risk clobbering a shared blob the way two overlapping reads of one JSON string could. This also let a good deal of machinery from the chrome.storage.local design go away entirely: the version-token optimistic-concurrency retry loop, the clearedAt-cutoff filtering, the incoming-message coalescing window, and the `gsAppendLogEntries` message action itself (removed from `INTERNAL_MESSAGE_ACTIONS`, its dedicated listener, and the guard for it in `background.js`'s own listener) -- IndexedDB's own per-record writes need none of it. `debug.js`'s `readLogBuffer()`/`readLogBufferFull()` now read from the new store (a backward cursor for the live view's most recent window, a plain `getAll()` for the full report), and the live view's full-buffer count uses a new cheap `countLogEntries()` (a native IndexedDB `count()`) instead of fetching every entry just to display a number. The old, now-orphaned `chrome.storage.local` keys (`gsLogBuffer`, `gsLogBufferFull`, `gsLogBufferVersion`, `gsLogBufferClearedAt`) are removed once on module load rather than left to sit there indefinitely. A Codex review round then caught two gaps in the new store itself. First (P1): gsIndexedDb.js's addLogEntries() caught its own write failures and reported them via gsUtils.error() -- which appends a new log entry and immediately triggers another flush, so a persistent failure (IndexedDB genuinely unavailable, a quota error) fed itself in a tight loop, logging and re-attempting forever, and the original batch was silently dropped rather than requeued since addLogEntries() resolved instead of rejecting. It now lets the failure propagate, so gsUtils.js's own _flushNowCore() catch requeues the batch and retries on its normal 1.5s schedule without routing anything back through the pipe that just failed. Second (P2): with multiple contexts flushing independently, insertion order (the autoIncrement primary key) doesn't match chronological order -- a throttled context can persist an older-ts batch after another context's newer entries already landed, with a higher key. Walking the store by primary key (fetchLogEntries()'s backward cursor, trimLogEntries()'s oldest-eviction) could then show stale entries as "most recent" or delete genuinely newer ones while keeping older ones. A new `ts` index on the store (added via a transaction.objectStore() upgrade path that also backfills the index onto anyone who'd already opened DB_VERSION 4 without it) is what every read/trim now orders and evicts by instead. A further Codex review round on the same store caught two more: addLogEntries() added each entry via a separate db.add() call, so a mid-batch failure (e.g. approaching a storage quota) still left every earlier entry in that batch committed in its own transaction even though the caller sees the whole call as failed and requeues the entire batch, duplicating those already-committed entries on retry -- now one shared readwrite transaction for the whole batch, rolled back together on any single failure. And clearLogEntries() swallowed its own failure and returned nothing, so gsUtils.clearLogBuffer() always resolved successfully regardless of what actually happened -- debug.js's "Clear log" button already checks the response's success flag specifically to show "clear failed", a path that could never trigger; both methods now propagate the real outcome. A further Codex review round caught two more, both in the IndexedDB migration itself. First: the one-off cleanup of the old chrome.storage.local keys ran from gsUtils.js's module-load code, executed in *every* context -- but a profile that had already accumulated a multi-MB gsLogBufferFull under the old design would have that removal itself be a chrome.storage.local mutation, and Chrome delivers a removed key's full oldValue to every onChanged listener in every context just like any other change, re-creating (once, on first load after upgrading) the exact broadcast-fanout problem this migration exists to eliminate. Moved to background.js's own service-worker init instead, bounding it to at most the two instances "incognito": "split" creates rather than every open context. Second, a real behaviour change worth documenting rather than fixing: chrome.storage.local wasn't partitioned by that same split (both instances shared one buffer under the old design), but IndexedDB is -- a regular debug.html session can no longer see what happened in an incognito window, or clear its buffer. Accepted as-is (bridging the two isn't practical without reintroducing some form of cross-context broadcast) and documented in gsUtils.js's log-buffer section. A further Codex review round caught one more, in the trimming mechanism itself: the per-context throttle (at most once a minute per context) still let every one of dozens of suspended tabs independently decide "trim needed" on its own first flush after a restore burst, each running its own 10,000-key scan and delete transactions -- a real CPU/IndexedDB-contention burst in the exact path meant to prevent one. Trimming is no longer triggered from the flush path at all: gsIndexedDb.js's new syncLogTrimAlarm() registers a single periodic `chrome.alarms` schedule (5 minutes) from background.js's own init, the same pattern already used for the backup and news-feed alarms, decoupling trimming entirely from how often, or from where, entries get logged. Live testing after all of the above then caught a real bug none of the review rounds did: the live debug view showed "No entries" while the entry counter kept climbing normally. IndexedDB only fires its upgrade handler when the *requested* database version is higher than the one already on disk -- adding the 'ts' index to DB_LOG_ENTRIES without also bumping DB_VERSION (on the assumption the transaction.objectStore() handling above would pick it up for anyone already at that version) meant upgrade() simply never ran again for a profile that had already opened version 4 earlier that same day, before the index existed. fetchLogEntries()'s index('ts') call failed outright against that on-disk database, caught by its own try/catch and returning an empty array, while countLogEntries() (a plain store-level count needing no index) kept working. DB_VERSION bumped to 5 to force the upgrade to actually run. Live testing then flagged a real performance regression: "Download report" took noticeably longer than before. fetchAllLogEntries() (backing the full downloadable report) read the whole store through the new 'ts' secondary index, walking that index's own B-tree one entry at a time for up to 10,000 entries -- real, measurable overhead next to the old design's single JSON.parse() of one blob. Switched to a plain getAll() on the primary store (a single bulk read) followed by an in-memory sort by 'ts', keeping the same chronological-order guarantee at a fraction of the cost.



- **`debug.html`'s `refreshLogs()` had no protection against overlapping calls, unlike `fetchTabInfo()` right above it in the same file — it re-runs on every window `focus` event (multi-monitor setups, rapid alt-tabbing), and every run did a full `chrome.storage.local.get()` + `JSON.parse()` of *both* the 500-entry live buffer and the up to 10,000-entry full buffer, the latter fetched purely to display a count** (`debug.js`): confirmed live via a genuine V8 out-of-memory crash reproduced just by using `debug.html` a bit with `captureLogs` on -- Crashpad's local minidump showed `v8-oom-lo-space-size` (V8's large-object space, not external/malloc memory) at ~3.8GB of a ~4.2GB cap, with `old-space-size` a tiny ~18MB, and critically the same ~3.7-3.9GB ceiling regardless of how many suspended tabs happened to be open (28 in one crash, 48-49 in two others) -- ruling out the earlier favicon-size theory (which would scale with tab count) and pointing instead at a burst of overlapping full-buffer parses from repeated focus events, piling up faster than GC could reclaim them. `refreshLogs()` now coalesces concurrent calls into at most one in-flight run plus one queued follow-up, the same guard shape `fetchTabInfo()` already uses for the identical class of problem.
- **`chrome.tabs.onUpdated`'s listener did real work (logging, a storage read, dispatching into both suspended/unsuspended tab handlers) for every tab-state change, including ones it never acts on — chiefly `'frozen'`, which live testing found Chrome flips on/off across background/suspended tabs constantly (over 4000 occurrences in a 43-minute session, with dense clusters of dozens within a few seconds)** (`background.js`): every one of those irrelevant events still reached `gsUtils.log()` (a real cost with `captureLogs` on: buffering, coalescing, periodic storage flushes) and both `tgs.handleSuspendedTabStateChanged()`/`handleUnsuspendedTabStateChanged()`, only for either to immediately discover there was nothing to do (both already early-return for a `changeInfo` without `status`/`discarded`/`audible`/`pinned`, which a `frozen`-only event never has). The listener now checks for one of those actually-relevant keys before doing anything at all, cutting logging and dispatch overhead entirely for events nothing here has ever acted on. Confirmed live via debug reports tying repeated Out-of-Memory crashes to dense `frozen` clusters (up to 39 within a few seconds) — this alone may not fully explain a crash at that scale, but removes real, previously-unnecessary load from every one of the thousands of these events in a typical session.
- **A burst of suspended tabs completing near-simultaneously (e.g. Chrome un-discarding several at once when a window regains focus after being idle) could crash the extension with an out-of-memory error, unrelated to session restore itself** (`tgs.js`): `chrome.tabs.onUpdated`'s `'complete'` handler calls `initialiseSuspendedTab()` independently for every tab, with no throttling — each call did its own storage reads, a `chrome.tabs.sendMessage()` (with up to ~6s of retries) to the tab's content script, and a `gsTabCheckManager` queue enqueue, all unbounded. Confirmed live via a genuine "Out of memory" crash (Chrome's own error page, not a caught exception — nothing in the extension's log buffer past the last normal entry) tied to a burst of 15-20 suspended tabs all firing `'complete'` within roughly 130ms of each other on window-focus regain, distinct from the already-bounded session-restore path (which staggers tab creation and was confirmed unaffected: a 47-tab restore completed cleanly in ~3.3s with every tab correctly recreated as a lightweight suspended placeholder, none as a real page load). `initialiseSuspendedTab()`'s actual work (message send + queue enqueue, not the explicit "reload to unsuspend" path, which stays instant) is now capped at 5 concurrent, so a burst like this gets spread out over time instead of firing every tab's full init work in the same instant. A larger, live reproduction of the same crash (46 tabs this time, not 15-20) then showed this concurrency cap barely helped: the actual memory- and IndexedDB-heavy work (favicon lookup, title/theme setup) runs inside *each suspended tab's own separate page context*, driven by the `'initTab'` message this function sends — `chrome.tabs.sendMessage()` resolves as soon as that page's own listener acknowledges it, a few ms, long before that page's own async work is actually done. Capping how many sends are *in flight* at once barely changes how many separate tab contexts end up doing real work at the same time, since each send-and-acknowledge round trip here was already fast regardless of the receiving page's own workload. Replaced with an actual dispatch-rate limiter: at most 5 tabs are sent their `'initTab'` message per 300ms, regardless of how fast those sends themselves resolve, spreading a 46-tab burst over several seconds of real wall-clock time instead of firing it almost all at once. A Codex review round then caught that this rate limiter didn't actually limit anything: it used "queue is non-empty" as the signal for whether a cooldown window was still needed, but a burst arriving as separate `chrome.tabs.onUpdated` events (not one synchronous batch) drained the queue back to empty after every single arrival, so the cooldown timer never armed and every call slipped through immediately regardless of the limiter. Replaced with a window counter that persists across `pump()` calls independent of queue state, so arrivals spread out one at a time are still correctly capped. A further Codex review round then caught that the underlying premise for this fixed-interval design was itself wrong: it assumed `chrome.tabs.sendMessage()` for `'initTab'` resolved almost instantly regardless of the receiving page's own work, so a batch released purely on elapsed time could still leave an unbounded number of tabs genuinely in flight at once whenever any single tab's real work outlasted the 300ms interval. Reading `suspended.js`'s own `handleMessageRequest()` directly (rather than inferring from log timing, which is what led to the wrong premise in the first place) confirms `sendResponse()` is only called *after* `await initTab(...)` completes — the message genuinely blocks on the real favicon/title/theme work finishing, not a quick acknowledgement. Reverted to a real concurrency bound: a job's slot is held for its actual duration and only released once the real work resolves, capping genuinely concurrent in-flight jobs at 5 rather than releasing a new batch on a fixed timer regardless of whether the previous one had actually finished. A further Codex review round then caught that a tab closing or navigating away while still *waiting* for one of those 5 slots (not yet started) stayed queued anyway — when its turn eventually came, it would still run the full `sendInitTabMessageWithRetry()` cycle (~6s of retries) against a tab that no longer existed, occupying a slot the whole time and delaying real, still-open tabs queued behind it; in a large burst, enough stale entries at the front could delay valid work for minutes, or leave it uninitialised entirely if the service worker recycled in the meantime. Queued entries are now tracked by tab ID, and `chrome.tabs.onRemoved` (already wired to `removeTabIdReferences()`) cancels any still-queued entry for that tab immediately instead of letting it consume a slot for no reason. A final Codex review round caught the remaining gap: an in-place navigation on a still-queued tab (an unsuspend command, address-bar entry) fires neither `onRemoved` nor `onReplaced`, so that cancellation path never triggers, and the stale entry would eventually send its old `'initTab'` payload to the new page (no receiver for it) while still occupying a slot for the full retry budget. `_runInitSuspendedTabLimited()` now cancels any existing queued entry for the same tab ID before enqueuing a fresh one, so a tab re-entering this path before its previous entry ever started always replaces it instead of leaving both around. A further Codex review round caught the remaining gap in the opposite direction: a tab unsuspending, or navigating away, while still queued for a slot doesn't re-enter this path at all — it routes to `handleUnsuspendedTabStateChanged()` instead once `gsUtils.isSuspendedTab(tab)` reads false, and neither `onRemoved` nor `onReplaced` fires for an in-place transition, so the stale entry was never cancelled. `handleUnsuspendedTabStateChanged()` now unconditionally cancels any queued entry for that tab ID itself — reaching that handler at all is already proof the tab is no longer suspended. Yet another Codex review round caught that this still missed one path: `background.js`'s dispatch only calls `handleUnsuspendedTabStateChanged()` when `gsUtils.isNormalTab(tab)` also passes, so a queued tab navigating to a "special" URL (`chrome://`, another extension's page) that `isNormalTab()` excludes fell through both handler branches entirely, skipping cancellation altogether. The cancellation call moved out of `handleUnsuspendedTabStateChanged()` and into `background.js`'s dispatch itself (a newly exported `tgs.cancelQueuedInitSuspendedTab()`), called unconditionally whenever `isSuspendedTab(tab)` reads false — covering the normal-tab and special-page cases alike. Still another Codex review round caught a narrower race none of the above touches: `initialiseSuspendedTab()` awaits several session-storage reads *before* ever calling `_runInitSuspendedTabLimited()` — if the tab navigates away during that window, the transition's cancellation call runs while no queue entry exists yet to remove, and the stale entry gets enqueued anyway once those awaits finish. `_runInitSuspendedTabLimited()`'s `run()` now re-fetches the tab and re-checks `gsUtils.isSuspendedTab()` immediately before starting the real work (covering both the "runs immediately" and "was queued, runs later" paths), instead of only ever checking at enqueue time. One more Codex review round then caught that even this wasn't the last word: the closure itself still awaits `gsStorage.getOption()` and `gsSession.getSessionId()` after that revalidation, its own race window with nothing left able to cancel the job once it's active (past the queue). The closure now re-fetches the tab and re-verifies it's still suspended (and still the same URL) immediately before the actual `sendInitTabMessageWithRetry()` call — the one point that actually matters — and uses that freshly-fetched tab for the payload instead of the one it originally captured, so a page that somehow still reads as suspended but changed underneath doesn't get initialised with the previous URL's stale title/favicon. One more Codex review round caught that the fix itself still had the exact same shape of gap: `sessionId: await gsSession.getSessionId()` ran *after* the revalidation, its own leftover async step the tab could go stale during. Restructured so every async prep step (`gsStorage.getOption()`, `gsSession.getSessionId()`) ran *before* the freshness check, making that check the last thing that could go stale before `sendInitTabMessageWithRetry()` followed immediately, no intervening `await`. A seventh Codex review round then caught a structurally different gap in the same class: a tab navigating away during one of `sendInitTabMessageWithRetry()`'s own retry delays (up to ~6s total) had nothing able to stop the recursion, since every fix so far only ever re-checked freshness at one specific checkpoint in `initialiseSuspendedTab()`, never inside the retry loop itself — and there's always one more await downstream of any single checkpoint. Six successive rounds finding a new checkpoint gap each time was itself the signal to stop patching checkpoints and close the whole class at once: a shared, mutable cancellation token (`_initSuspendedTabTokenByTabId`, keyed by tab ID) now lives for a job's entire lifetime, queued *or* running. `_cancelInitSuspendedTab()` (renamed from `_cancelQueuedInitSuspendedTab()`, called from the same three places as before — re-entry, `removeTabIdReferences()`, and `background.js`'s dispatch) flips that one token the instant a tab is detected as no longer suspended, and every checkpoint along the job's path — including now inside `sendInitTabMessageWithRetry()`'s own retry loop — consults the same token instead of re-deriving freshness locally at each new await someone happens to find. A Codex review round on the token itself then caught a small leak: cancelling a still-queued job marked its token cancelled but never removed it from the map, since release() (the only place that deletes it) only ever runs for a job that actually started -- repeated restore/navigation bursts could accumulate one stray map entry per tab cancelled while still queued for the remainder of the service worker's lifetime. _cancelInitSuspendedTab() now deletes the token immediately when it also found (and removed) a queued entry for the same tab ID, since _runInitSuspendedTabLimited() never leaves both a queued entry and an active job's token current for the same tab at once. A further Codex review round caught a distinct edge case none of the token machinery covers: a tab Chrome discards while still showing its suspended URL stays isSuspendedTab() === true (the URL never changes), so background.js keeps routing its onUpdated events through the suspended branch -- cancelInitSuspendedTab() (only reachable from the non-suspended branch, re-entry, and removeTabIdReferences()) never sees it, and the freshTab revalidation happily accepted a discarded tab too. The closure now also bails out on freshTab.discarded, with nothing further to reschedule -- reloading a discarded tab gets its own fresh loading/complete cycle, and therefore its own fresh call into this same function, whenever that happens.' Yet another Codex review round caught that this one-time check only protects the instant right before the first send: a discard landing after that check -- e.g. during one of sendInitTabMessageWithRetry()'s own retry delays -- had nothing left able to stop the job short of the full ~6s budget. handleSuspendedTabStateChanged() now cancels the shared token directly on changeInfo.discarded, reaching the same token sendInitTabMessageWithRetry()'s retry loop already checks on every attempt, regardless of which one it's currently in. A further Codex review round caught that cancellation stopping the send wasn't the end of it: execution still fell through to an unconditional queueTabCheck() afterward -- a responsiveness check for a page expected to already be running, which would resolve the still-discarded tab as unresponsive after its own delay and reload (wake) it via resuspendSuspendedTab(), undoing the very discard this job just deferred to. The closure now also returns before that queueTabCheck() call when token.cancelled.
- **`checkQueue`'s own responsiveness check could throw an uncaught exception, stranding one of its (deliberately few) concurrent slots for up to 60 seconds instead of failing promptly** (`gsTabCheckManager.js`, `gsTabQueue.js`): confirmed live via the extension's own error console — `chrome.tabs.sendMessage(tab.id, { action: 'getSuspendInfo', tab })` in `checkSuspendedTab()` can resolve successfully with `undefined` instead of rejecting (e.g. if the tab navigates/reloads in the narrow window between the message arriving and `sendResponse()` being called), and the very next line read `suspendInfo.sessionId` with no guard, throwing `Cannot read properties of undefined`. Worse, `gsTabQueue.js`'s `processTab()` called its `executorFn` (the function that threw) with no `try`/`catch` at all — explicitly commented `// async. unhandled promise` — so the failure never reached anything that could resolve, reject, or requeue the job; the slot just sat in `STATUS_IN_PROGRESS` until the job's own `jobTimeout` (60s default) eventually fired via a separate timer. With only 3 concurrent executors on this queue, repeated occurrences could meaningfully choke its real throughput for extended periods. Both gaps are now closed: `checkSuspendedTab()` guards against an empty `suspendInfo` and requeues promptly (same as the already-handled rejection case just above it), and `processTab()`'s `executorFn`/`exceptionFn` calls are now wrapped so any unexpected throw fails the job immediately instead of waiting out the full timeout. A Codex review round then caught that the initial version of this fix rejected the job directly on an unexpected throw, rather than routing it through the queue's own configured `exceptionFn` (the same one the timeout path already uses, which resolves `false` rather than rejecting) — a caller aggregating many of these jobs via `Promise.all()`, like `performInitialisationTabChecks()` at startup, would have the whole aggregate abort on one queue-internal failure, skipping that caller's own post-await cleanup (removing its temporary listener, restoring queue properties) and potentially leaving startup permanently stuck in "initialising" state. Both the executor-throw and exceptionFn-throw paths now route through the same caller-safe `resolve(false)` contract instead.
- **A single suspended tab with a large-resolution favicon (e.g. a site serving a 512×512 apple-touch-icon as-is) could push V8's shared per-process memory allocator toward its hard ~4GB cap, crashing the whole browser process once enough suspended tabs piled up in it** (`gsFavicon.js`, `gsUtils.js`): confirmed via a real, reproducible crash — Crashpad's local minidump for a "V8 fatal error: Out of Memory" showed `view-count`/`web-frame-count`: 49, all sharing one origin (`chrome-extension://…`), since Chrome puts every same-origin extension page (every open `suspended.html` is the same origin) in one shared renderer process with one shared V8 heap. The dump's `v8-oom-old-space-size` (regular JS objects, including the log buffer) was a tiny 18MB, while `v8-oom-memory-allocator-size` (external/backing-store memory: canvas pixel buffers, typed arrays) sat at 3.7GB of a 4.2GB cap — the smoking gun pointing at binary image data, not logging. `gsFavicon.js`'s `buildFaviconMeta()` sized its working canvas to the source image's *native* resolution before calling `getImageData()`, allocating two full-size `Uint8ClampedArray` copies and two `toDataURL()` PNG encodes — cost that scales with the source image's pixel count, even though the result (`suspended.js`'s `setFaviconMeta()`) is only ever shown as a tiny tab-bar icon. That per-tab cost multiplies across every suspended tab sharing the same renderer process. The working canvas is now capped to a 128px max dimension (downscaled via `drawImage()`, same pattern the file's other, already-capped `createImageFingerprint()` uses), bounding the cost regardless of the source image's actual resolution. Separately, `gsUtils.js`'s log-buffer merge (`_mergeAndPersistCore()`, added in #464-#467) re-sorted the *entire* persisted buffer (up to `_LOG_BUFFER_FULL_MAX` = 10,000 entries) on every single merge, even though it's typically already sorted and only a small new batch is being appended — real, avoidable CPU/allocation churn on every flush with `captureLogs` on. A new `_appendSorted()` helper skips the full-array sort whenever the new batch's timestamps already sort after the existing buffer's (the common case), falling back to a full sort only for the rare genuinely out-of-order merge. A Codex review round then caught that the cap alone left every already-suspended tab exposed indefinitely: `getFaviconMetaFromCache()` returns an IndexedDB-persisted `faviconMeta` as-is, only fingerprint-validating it, with no check on its dimensions — so profiles that had already accumulated an oversized favicon (precisely the ones most likely to have triggered the crash) would keep decoding it at native resolution in every suspended tab even after updating. A new `FAVICON_META_VERSION` stamped onto every freshly-built `faviconMeta` and checked in `isFaviconMetaValid()` makes any pre-existing cache entry (built before this cap existed) read as invalid, so it falls through the normal cache-miss path and gets rebuilt — and re-saved — with the new capped logic, self-healing existing profiles over time as suspended tabs are naturally revisited.
- **Debug page: tabs whose content script stopped responding (page still alive, distinct from a discarded tab) stayed stuck reporting "unknown" indefinitely, surviving repeated page reloads** (`debug.js`): `getDebugInfo()` probed a "normal" tab's content script directly via `sendRequestInfoToContentScript()`, and on failure fed a `null` status into `calculateTabStatus()`, which *itself* attempted the exact same probe a second time — pure wasted duplicate work — before falling back to `STATUS_UNKNOWN` on a second failure, with no attempt to reinject the content script. Only `tgs.js`'s own periodic `checkQueue` responsiveness checks ever did that reinjection, entirely independent of this page, so a tab that broke between two of those scheduled runs had no way to recover via the debug page at all; confirmed via live testing, several tabs stuck reporting "unknown" for many minutes across repeated manual reloads. `getDebugInfo()` now routes the check through `gsTabCheckManager.queueTabCheckAsPromise()` — the same responsiveness-check queue `checkQueue` itself uses, reinjection attempt included — instead of a one-shot probe, and `calculateTabStatus()` no longer needs its own redundant attempt, since it's given the already-resolved status directly. Every page (including this one) gets its own separate `gsTabCheckManager` module instance, so `debug.js` now also calls `gsTabCheckManager.initAsPromised()` during its own init (the same pattern `health.js` already used for its own tab-suspend/check needs) — without it, `queueTabCheckAsPromise()` would silently hit the "queue not initialized" guard and resolve `STATUS_UNKNOWN` immediately every time, skipping the reinjection attempt entirely. A Codex review round then caught that the recovery still wasn't visible where it mattered: a tab needing actual reinjection routinely takes longer than the 500ms per-tab render deadline `fetchTabInfo()` already enforces, so that render still (correctly) shows "unknown" for it — but the late result, once it did arrive, was simply discarded, since only the raced `promiseWithTimeout()` value ever reached the table. The row stayed stuck showing the timeout fallback until some unrelated later refresh happened to run. Each tab's `<tr>` now carries a `data-tab-id` attribute, and the underlying (unraced) info promise patches that one row in place once it resolves, so a tab that recovers mid-check updates live instead of waiting for the next full profiler refresh. A further Codex review round caught two more, both real races the live-update fix above introduced. First: a late update from an *older* pass could overwrite a *newer* pass's already-correct row for the same tab (e.g. it genuinely became pinned in between) with stale data, since nothing tracked which pass a given row's data actually came from; every pass now stamps a generation counter, and a late update only applies if its own pass is still the newest one. Second, and more serious architecturally: this page's own separate `gsTabCheckManager` instance (`initAsPromised()`'d locally, added above) has no visibility into a recovery the *service worker's own* queue might already be running for the same tab (e.g. `tgs.js`'s `handleTabFocusChanged()`) — two independent queues could both decide to reinject the same tab's content script at once, and reinjection is already documented (`gsTabCheckManager.js`'s own comments) as leaving old runtime listeners active when that happens. The check is now routed through a new `checkTabResponsiveness` message, handled in `background.js` by calling the service worker's own singleton `gsTabCheckManager` queue instead — the same one everything else in the extension already funnels through — and `debug.js` no longer creates (or initialises) its own separate instance at all. A final Codex review round caught that the new `checkTabResponsiveness` message wasn't marked service-worker-only: `chrome.runtime.sendMessage` with no `tabId` broadcasts to every open extension page, so `options.html`/`updated.html`'s own listeners (which didn't know this new action, unlike `background.js`) could resolve first with an unhandled-message response, racing and beating the service worker's real, slower one — leaving `calculateTabStatus()` falling back to the old one-shot probe with the background-queued recovery no longer attached to the live row update. Added `checkTabResponsiveness` to the existing `gsUtils.INTERNAL_MESSAGE_ACTIONS` set every page's `messageRequestListener` already consults for exactly this class of problem.

  On top of the review-driven fixes above, both `fetchTabInfo()` and `buildReport()` fired their whole per-tab batch via a plain `Promise.all(tabs.map(...))`, with no cap on how many ran at once — fine for a handful of tabs, but a profile with hundreds of them open would fire hundreds of concurrent `chrome.tabs.get()`/`chrome.alarms.get()` calls, and (for every "normal" tab) hundreds of simultaneous `checkTabResponsiveness` messages to the service worker, all in one instant, on every single profiler refresh or report generation. A new `mapWithConcurrency()` helper caps both to at most 20 tabs in flight at once, spreading that same total work out over time instead of bursting it — the debug page (and this whole log-buffer/tab-check machinery it now exercises) should stay usable rather than becoming the thing that finally tips a heavily-loaded profile (hundreds of tabs open) into crashing.

  A Codex review round then caught two consequences of that same batching change. First, in `fetchTabInfo()`: with concurrency-limited batches, a tab's real result can now land in the gap between "no row exists yet" (the initial render hasn't happened) and "the final render already ran" — the live per-row patch is a silent no-op in that gap, and without also tracking the fresher value elsewhere, the eventual render still baked in that tab's stale timeout fallback despite already knowing better. `debugInfos` is now populated in place from the same unraced result the live patch already uses, so the final render always reflects whichever is freshest. Second, in `buildReport()`: the service worker's own `gsTabCheckManager` queue has only 3 concurrent executors, so with more than a handful of "normal" tabs, the existing 500ms per-tab deadline expired for most of them well before the queue even got to processing them — not because they were unresponsive, but because they were still waiting their turn — and unlike the live table, a generated report has no later-refresh path to correct a wrong "unknown" afterwards; it freezes whatever it captured permanently. Report generation dropped the per-tab timeout entirely (the queue's own 60s job timeout is still the real upper bound) — it's a deliberate one-off action, unlike the live table, so it's fine for it to take longer in exchange for actually being correct.
- **Debug page: repeated `focus` events could stack overlapping full tab-info refreshes, flooding the log and hammering every open tab** (`debug.js`): `window.addEventListener('focus', () => { fetchTabInfo(); refreshLogs(); })` had no debounce or re-entrancy guard, but Chrome/the OS can genuinely fire several `focus` events on this page in quick succession (multi-monitor setups, rapid alt-tabbing, etc.). Each one kicked off its own full `fetchTabInfo()` run, and every one of those calls `getDebugInfo()` — including a real `sendRequestInfoToContentScript()` round trip for every "normal" (non-suspended) tab — for every currently open tab again, all overlapping. Confirmed via a live debug report spanning several days of `captureLogs` left on: a burst of duplicate `getDebugInfo`/`getDebugInfo callback` log lines for the same tab within the same millisecond, repeated roughly once per stray focus event, on top of everything already fixed in the log-buffer rework — a genuinely separate cause of both the sluggishness and repeated crashes. `fetchTabInfo()` now skips re-entrant calls while one is already in flight, since a later call arriving mid-refresh is redundant anyway. A Codex review round then caught that dropping a re-entrant call outright is also wrong: if the user left and came back (opened/closed/changed a tab) while a run was still in flight, that run's `tabsQuery()` snapshot was already taken before the change, so it renders stale by the time it finishes, and the profiler stayed stale until another focus event happened to occur. A re-entrant call now queues one follow-up run instead of dropping outright, with further calls arriving while that follow-up is already queued still coalescing into it — a rapid burst still produces at most one extra run, not one per stray event.
- **Debug page: `options.html`/`updated.html` could still shadow the service worker's `checkTabResponsiveness` response even after that action was added to `INTERNAL_MESSAGE_ACTIONS`, and a tab closing mid-check could stall the whole report** (`options.js`, `updated.js`, `debug.js`, #472 review): both pages' `messageRequestListener` were declared `async`, so their early `return false;` decline of this service-worker-only action was still wrapped in a resolved Promise — Chrome/Firefox can treat a listener's returned Promise as its actual response, letting that trivial resolved value race and beat the service worker's real, slower result. Both listeners are now synchronous functions, matching the pattern `background.js`/`suspended.js` already use. Separately, `getDebugInfo()` left its callback uninvoked when a tab closed between `tabsQuery()` and its own `chrome.tabs.get()` lookup — the async function's rejection had nothing catching it, leaving `fetchTabInfo()`'s/`buildReport()`'s manually constructed Promise pending forever now that the per-tab timeout was removed from the report path; it now resolves an unknown-status fallback instead. That fallback also surfaced a latent crash in `generateTabInfo()`, which read `info.tab.url` without checking `info.tab` first when no cached favicon data URL was available. A further Codex review round then caught that making `options.js`'s listener synchronous exposed a separate, real bug: `tgs.js`'s `handleNewStationaryTabFocus()` awaits an `initSettings` message to `options.html` before resetting the previously focused tab's suspend timer, but that case never called `sendResponse()` — with the listener now returning literal `true` (rather than an async function's implicitly-resolving Promise), the message channel stayed open with nothing ever responding, hanging that await until the channel itself tore down and leaving the previous tab's timer un-reset in the meantime. `sendResponse()` is now called unconditionally after the switch, since every case there is already fully synchronous. A further Codex review round then caught that calling it unconditionally overcorrected: on the Chrome versions this manifest still supports (110-121), an async listener's returned Promise wasn't treated as a response at all, but this synchronous listener's unconditional `sendResponse()` now actively answers *every* action outside `INTERNAL_MESSAGE_ACTIONS` with `undefined` — including ones this page doesn't own, like `debug.js`'s `repairFavicons` (handled only in `background.js`), where this stray response could beat the real, slower one and make the debug UI report `failed` despite the repair actually succeeding. `sendResponse()` now only runs in the `initSettings` case; the `default` case goes back to a real `false` decline instead. A further Codex review round then caught a separate bug in the concurrency-limited batching itself, unrelated to the message-listener issues above: each `mapWithConcurrency()` worker released its concurrency slot as soon as the 500ms render deadline passed, even though the underlying `infoPromise` (and its `checkTabResponsiveness` message to the service worker) kept running in the background. With hundreds of "normal" tabs slower than 500ms, `mapWithConcurrency()` raced ahead into the next batch on every one of those releases while the slow tabs' real checks stayed in flight — batch after batch piling up hundreds of in-flight messages and open response channels against the service worker's own queue (only 3 concurrent executors), exactly the burst this concurrency cap exists to prevent. Each worker now still `await`s the real `infoPromise` before releasing its slot, keeping the 500ms deadline scoped to only what gets rendered in that pass. A further Codex review round then caught that this fix traded one problem for another: holding the slot until the real check settled meant `mapWithConcurrency()`'s own returned promise — which `fetchTabInfo()` awaits before rendering anything, and which the page's own init flow awaits before attaching its controls — now didn't resolve until *every* tab's real, possibly multi-second-with-reinjection check finished, not just the 500ms race. On the very first load of a profile with more than a handful of slow "normal" tabs, this could leave the whole profiler table (and page controls) blank and inert for many seconds. The concurrency limiting moved to where it actually matters — `getDebugInfo()` now gates only the `checkTabResponsiveness` message dispatch itself (the expensive, bursty part) through a small dedicated semaphore (`withResponsivenessLimit()`), not the whole per-tab flow — so `fetchTabInfo()` no longer needs `mapWithConcurrency()` at all: each tab races its own `infoPromise` against the 500ms deadline independently via a plain `Promise.all()`, and that race settles promptly regardless of how many other tabs are still queued behind the semaphore for their real check.
- **Log buffer: a burst of near-simultaneous flushes from many open contexts (e.g. every suspended tab reacting to `captureLogs` being toggled) could make the whole extension sluggish, including unrelated page loads** (`gsUtils.js`): every `gsAppendLogEntries` message costs one full get+parse+stringify+set of the entire shared buffer (up to `_LOG_BUFFER_FULL_MAX` entries) — `chrome.storage.local` has no append primitive, only whole-value writes. A broadcast every open context reacts to at once (toggling `captureLogs` itself, which every suspended tab logs as "ignoring unhandled message") could land dozens of these messages within a few milliseconds of each other; merging each one separately serialized dozens of full-buffer round trips back to back through `_writeQueue`, each one's synchronous JSON work blocking this single-threaded context in turn — including its ability to promptly respond to anything else, like whatever `options.html`/`debug.html` needed from it on their own load. Confirmed via live testing with ~50 suspended tabs open and `captureLogs` on: reopening the debug/options pages became noticeably slow, worse than before this rework began. Incoming messages are now coalesced into one merge per 250ms window instead of one per message, turning a 50-tab burst into a single round trip; the live/report buffer size caps (`_LOG_BUFFER_MAX`/`_LOG_BUFFER_FULL_MAX`) are left unchanged, since a user with many tabs open is exactly the case where a shorter history would hurt diagnosing an issue the most — the coalescing alone removes the multiplier, independent of buffer size. A GitHub Codex review round then caught a severe regression the coalescing itself introduced: if many contexts had each independently accumulated up to their own `_pendingEntries` cap (5,000) while the service worker was unreachable, all flushing once it came back reachable could aggregate well over 100,000 entries into a single coalesced batch — and `_mergeAndPersistCore()` appended it via `current.push(...freshEntries)`, a variadic spread that turns every element into its own function argument and throws a `RangeError` in V8 past a few tens of thousands of them. The merge then failed, every sender requeued its batch, and the next coalesced window reproduced the exact same oversized aggregate, indefinitely — a real, reproducible crash matching a live report of the extension crashing after `captureLogs` had been left on for a while, buffer around 5,000 entries. Fixed two ways: `_mergeAndPersistCore()` now appends via `concat()`, which has no argument-count limit regardless of array size, and `_flushIncomingBatch()` now trims the aggregated batch itself down to `_LOG_BUFFER_FULL_MAX` (keeping the newest) before it's ever merged, since carrying more of it into the merge than the persisted buffer could retain anyway was pure waste. A local adversarial-review round then flagged that trimming only at flush time still let the aggregate grow unbounded, even if briefly, throughout the whole 250ms window while messages keep arriving — the exact CPU/memory spike this coalescing exists to avoid, just delayed a beat. `_incomingBatch` is now capped on every single arrival (not just once before merging), and appended via `concat()` rather than a spread there too, since a single sender's own entries array can already be large enough (up to its own 5,000-entry cap) to risk the same argument-count limit on its own. Another GitHub Codex review round then caught an ordering race the coalescing design introduced: the service worker's own logging (`error()` in particular, which flushes immediately and bypasses the incoming-message coalescing entirely) merges through `_mergeAndPersistCore()` via a separate call path from a page's coalesced batch, and both end up serialized by `_writeQueue` in whichever order they happened to be *called*, not the order their entries were actually logged in — a page's batch that had been waiting out its 250ms window could land after a genuinely newer service-worker-local error, and since the buffers trim from the front assuming it holds the oldest entries, that trim could then evict the newer error instead of the older batch. Both buffers are now re-sorted by `ts` after merging and before trimming, so the front is guaranteed to be chronologically oldest regardless of which call path landed first. Yet another GitHub Codex review round then caught that the two earlier, pre-merge trims (capping `_incomingBatch` on arrival, and again before it's handed to the merge) still trimmed by arrival order rather than `ts` — a backlogged context that reconnects last, contributing older entries, could evict another context's genuinely newer entries before they ever reached the sort-then-trim inside `_mergeAndPersistCore()`, permanently losing them (every sender still gets acknowledged with success, since nothing detects the loss). Both pre-merge trims now go through a shared `_keepNewestByTs()` helper that sorts by `ts` before keeping the newest, the same rule the merge itself now applies.
- **Log buffer: unbounded in-memory growth with `captureLogs` left on, plus a genuine memory leak when the service worker stays unreachable** (`gsUtils.js`): every page (including every open suspended tab) ran its own copy of `gsUtils.js` and, on every single log call, pushed the entry into two in-memory arrays capped at 500/10,000 entries — but `debug.js`'s live view and downloadable report both read straight from `chrome.storage.local` instead, and the two exported accessors for those arrays (`getLogBuffer()`/`getLogBufferFull()`) had no callers anywhere in the codebase. Dead weight: every context, including every suspended tab, could end up holding up to 10,000 duplicated log-entry objects in memory that nothing ever read, for as long as `captureLogs` stayed on. Removed both arrays and their accessors entirely; only `_pendingEntries` (the small, normally-empty-within-1.5s delta awaiting its next flush) remains per context. Separately, and more seriously: `_pendingEntries` itself had no upper bound — if the service worker (or `chrome.runtime` in a page context) stayed unreachable, every failed flush requeued its batch while new log calls kept appending more, with nothing ever shrinking it, so heavy logging in that state could grow it without limit until Chrome killed the page or the extension itself for memory pressure, matching a real "insufficient memory, extension disabled, needs reload" report. `_pendingEntries` is now capped at 5,000 entries, oldest dropped first, at every point it can grow (a new log call, or a failed flush requeuing its batch). A Codex review round then caught that the cap could trim the wrong half: `gsUtils.error()` calls `_flushNow()` immediately, bypassing `_scheduleFlush()`'s "only one timer pending" guard, so an error-triggered flush can overlap with an already in-flight scheduled one; their requeue-on-failure steps could then complete in either order, and since `unshift()` always lands at the front regardless of which batch is actually older, the cap's front-trim (which assumes the front is the oldest entries) could end up discarding more recent diagnostics while keeping stale ones. `_flushNow()` now chains every call through one promise (the same pattern `gsUtils.js`'s own `_writeQueue` already uses for the service worker's storage writes), guaranteeing one flush's requeue fully lands before the next one starts. A local adversarial-review round then caught that the entry-count cap alone doesn't bound memory: `_capPendingEntries()` limits how many entries can pile up, but not how large any single one is — a call site logging a large non-data-URL string or object repeatedly (`_redactDataUrls()` only catches `data:` URLs) could still push significant memory through even a handful of entries. `_appendEntry()` now truncates any single message past 4,000 characters (marked as truncated, with the original length noted) before it's ever buffered, bounding each entry's own footprint independently of how many of them exist.
- **`.gitignore` didn't actually ignore the local OAuth client-secret file** (`.gitignore`): the gitignored-by-convention, never-committed file holding the PKCE Drive-auth client secret (#437) had no matching rule, so it showed up as a plain untracked file ready to be swept into a careless `git add -A`. Added, along with `/memory` (a local notes directory, same reasoning). Superseded later in this same release by the `gsOauthSecrets.js`/`gsOauthSecrets.local.js` split described above, but the underlying `.gitignore` gap this fixed (an ignore rule that looked right but silently didn't match) was real and worth keeping as its own note.
- **Log buffer: eager full-buffer load in every page, concurrent writers clobbering each other's entries, captureLogs never applying outside the service worker, stale captureLogs in already-open pages, a self-sustaining broadcast loop, an unhandled split-incognito writer, a collision-prone version counter, a Clear-log race, silent data loss on persistent failures, an async guard racing its own dedicated listener, an incomplete Clear-vs-merge race, a clear failure that could permanently wedge the write queue, a still-incomplete Clear-vs-merge purge, a corrupt buffer that could wedge retries forever, Clear's own unconditional remove able to delete a legitimately newer merge, a non-array parsed buffer able to wedge retries the same way, a failed cutoff write that could still report Clear as successful, a malformed array element able to wedge retries the same way again, a failed clear never actually reaching the debug page, and an unhandled rejection able to leave a message channel hanging** (`gsUtils.js`, `background.js`, `debug.js`, `options.js`, `suspended.js`, `updated.js`, #463/#464 review): twenty-two issues in the log-buffer rework, found across thirteen rounds of Codex review. `gsUtils.js` is imported by every page including `suspended.js`, so every context (service worker, every suspended tab, options, debug, etc.) gets its own separate module instance with its own in-memory buffer. `_ensureBufferLoaded()` used to hydrate that buffer from persisted storage (up to 10,000 entries) eagerly at module load, so every open suspended tab loaded the whole thing into memory just by being open, working against the extension's whole point; removed, since `_flushNow()` only actually needs it lazily. `clearLogBuffer()` cleared the in-memory arrays and fired off `chrome.storage.local.remove()` without awaiting it, racing an in-flight hydration that could repopulate the arrays with the old entries right after. The persisted `captureLogs` flag was restored only in `background.js`, so `gsChangelog.js`'s diagnostic `gsUtils.warning()` calls (added specifically to make changelog-modal failures diagnosable) never buffered anything running in `options.js`'s context, since that context's own `captureLogs` copy never got restored; and toggling captureLogs on the debug page only messaged the service worker, so an already-open page kept whatever value it loaded with. Fixing those two surfaced the deepest issue: with multiple contexts actually flushing now, each one overwrote storage with its own full local snapshot (or, in an intermediate append-merge attempt, still raced another context's uncoordinated read-modify-write), so two contexts logging around the same time could silently erase what the other had just written, and chrome.storage has no compare-and-swap to fix that with more retries. Settled on a single-writer design instead: only the service worker (`_isServiceWorker`, detected via `self instanceof ServiceWorkerGlobalScope`) ever touches these two storage keys; every other context hands its pending entries to it via a `gsAppendLogEntries` runtime message instead of writing directly. Inside the service worker, every write (a merge or a clear) is chained through one `_writeQueue` promise, so its own scheduled flush and an incoming message can no longer interleave their storage round trips either. `clearLogBuffer()` (only ever called from the service worker) now goes through that same queue, so it's correctly ordered against anything already merged or in flight instead of racing it. The `captureLogs` restore moved into `gsUtils.js` itself so every context picks up the persisted value on load, and a `chrome.storage.onChanged` listener there keeps it in sync live afterward, no per-page wiring needed for either. A fourth round then caught a self-sustaining loop the single-writer message actually introduced: `chrome.runtime.sendMessage` with no `tabId` broadcasts to every page, so every open suspended tab's (and `options.js`'s and `updated.js`'s) own message listener received the new `gsAppendLogEntries` action too, logged it as unhandled, and that log entry itself needed flushing via another broadcast, without end. Every page listener (plus `background.js`'s own separate one, which also received the broadcast alongside `gsUtils.js`'s dedicated handler for it) now silently ignores `gsAppendLogEntries` and `clearLogs` via a shared `gsUtils.INTERNAL_MESSAGE_ACTIONS` set instead of logging them as unhandled. That round also flagged that `manifest.json` declares `"incognito": "split"`, so a regular and an incognito window each run a fully independent service worker with its own `_writeQueue`, sharing the same storage but unaware of each other, defeating the "single writer" assumption for that one supported case; `_mergeAndPersist()` now stamps a `gsLogBufferVersion` counter on every write and re-checks it immediately after, retrying the merge (up to 5 attempts) on top of whatever the other worker just wrote if it changed underneath. A fifth round then caught that the counter itself couldn't actually prove which worker's write had landed: two workers racing off the same prior value both compute the same next number, so whichever `set()` happens to land last leaves a value both sides' verification reads back as a match, even though only one worker's batch is actually still in the buffers, silently losing the other one. Replaced the incrementing counter with a random per-attempt token (`crypto.randomUUID()`), so a worker's verification read now checks for its own exact token rather than a number any concurrent writer could equally have produced; a mismatch unambiguously means someone else's write landed, at which point the retry reads that fresh state and reapplies the batch on top of it. The same round (via a local adversarial review alongside GitHub's) then flagged the edge case a previous round had deliberately left as a disclosed limitation instead of engineering away: a page's batch already grabbed from its own `_pendingEntries` (or still mid-flight) at the exact moment "Clear log" runs could still land, and get persisted, right after the clear completed, resurrecting the entries the user just wiped — cancelling a message already in transit isn't something the extension messaging API offers a hook for. `_clearPersisted()` now also writes a `gsLogBufferClearedAt` timestamp (never removed by the clear itself), and every merge attempt drops entries older than it before persisting, regardless of which side of the clear the merge physically lands on. The same round also caught that a batch failing to persist after all 5 version-conflict retries, or a service worker unreachable mid-flight, silently discarded that diagnostic history for good; `_flushNow()` now requeues a failed batch at the front of `_pendingEntries` and lets the next scheduled flush retry it instead of losing it. A sixth round caught three more: `background.js`'s `messageRequestListener` was still declared `async` at the top (a separate instance of the same "async listener returns a Promise instead of the literal `true`/`false` Chrome needs" issue as elsewhere in this rework), so its early, synchronous-looking `return false;` for `gsAppendLogEntries` was actually wrapped in a Promise that could resolve before `gsUtils.js`'s own dedicated (and slower, since it does real storage I/O) listener responded; the sender keeps only whichever response arrives first, so `_flushNow()` could see that stray `false`, requeue an already-successfully-persisted batch, and resend (and re-persist) it every 1.5s indefinitely. `messageRequestListener` is now a synchronous function that returns `true`/`false` for real, running its async body in an internal IIFE. Second, the new `gsLogBufferClearedAt` cutoff only filtered the entries being merged, not the buffer state they were merged onto: if a merge's `get()` landed between Clear's two writes (the timestamp, then the buffer removal), it would still read the pre-clear buffer, and if its own `set()` then landed after Clear's removal, it silently reintroduced that entire pre-clear buffer while still verifying its own write as successful. `_mergeAndPersist()` now also re-checks `gsLogBufferClearedAt` as part of verification, retrying if it moved since the read, not just checking the write token. Third, `_clearPersisted()` had no error handling around its two storage calls; a rejected `chrome.storage` call there would leave `_writeQueue` itself a rejected promise, and since every future `_mergeAndPersist()`/`_clearPersisted()` call chains onto it with `.then()` and no rejection handler, that one failure would silently wedge all logging (merges and clears alike) until the service worker next restarted. Wrapped in `try`/`catch` to keep the queue alive across a failed clear, matching the resilience `_mergeAndPersist()` already had. A seventh round caught two more, both in the same function: the previous round's `clearedAt` re-check only re-filtered the incoming `entries`, not the persisted `current`/`currentFull` themselves — a worker whose earlier attempt had already written a stale pre-clear snapshot to storage (see the sixth round above) would, on noticing the clear and retrying, filter its own `entries` down to nothing and return early without ever purging that already-written stale snapshot, so Clear could still end up undone. Every attempt now re-filters the persisted buffers against `clearedAt` too, and writes the cleaned result even when the incoming batch itself has nothing new to add. Separately, a corrupt persisted buffer (malformed JSON) threw on every one of the 5 retry attempts without ever being recovered, so `_flushNow()` requeued the same unpersistable batch indefinitely, retrying roughly every 1.5s forever and blocking every later log entry from ever being persisted either. Each buffer is now parsed defensively and replaced with an empty array if corrupt, the same recovery the old (now-removed) buffer loader already had. An eighth round then caught that Clear's own final step could still be the one undoing correctness, from the opposite direction of every round before it: `_clearPersisted()` wrote the `clearedAt` cutoff, then unconditionally `remove()`d the buffer keys outright — if another worker's merge landed, filtered against that same cutoff, and successfully persisted its (legitimately newer) batch in the gap between those two steps, the unconditional `remove()` deleted that already-verified batch anyway, since a raw `remove()` has no way to tell "old" apart from "just landed". `_clearPersisted()` no longer removes the buffer keys directly at all: after writing the cutoff, it now calls `_mergeAndPersist([])`, reusing the exact same filter/write/verify/retry machinery a normal merge already goes through, so "clearing" and "merging" become the same timestamp-filtered operation and can no longer race each other in a way that loses data either direction. A ninth round then caught that the previous round's malformed-JSON recovery only handled `JSON.parse` actually throwing, not syntactically valid JSON that isn't an array (`null`, `{}`, etc., however that got into storage) — the same "corrupt buffer" failure mode from two rounds ago, just via a path the earlier fix didn't cover, with the same wedged-retries-forever consequence; `parseBuffer()` now falls back to `[]` for any non-array result too, not just an outright parse failure. That round's other finding is a genuinely different kind of issue, not a refinement of the concurrency mechanism the previous eight rounds were all narrowing down: `gsLogBufferClearedAt`, and every entry's own `ts`, both come from the wall clock, so a clock correction that moves the system time backward after a clear would make every subsequently-logged entry look older than the cutoff and have it silently (and indefinitely, until the clock catches back up) discarded by the exact same filtering this whole mechanism relies on. Deliberately left as a known, disclosed limitation rather than fixed here: replacing a wall-clock cutoff with a clock-independent generation/sequence marker is a larger, more invasive change than anything else in this pass, the failure mode (a machine's clock being corrected backward, immediately followed by "Clear log") is narrow, and the impact is limited to the debug-only log buffer, not any tab-suspend behaviour. A tenth round then caught that the previous round's unification (Clear now calls the same merge machinery as a normal write) had a gap of its own: if the `gsLogBufferClearedAt` write itself failed, the code still unconditionally went on to the purge step, which filtered against whatever cutoff was already in storage (unchanged, since the new one never landed), found nothing to purge, and reported success — so a failed Clear looked identical to a successful one from `clearLogBuffer()`'s side, and the debug page would refresh showing every old entry still there despite believing it had just been cleared. The purge now only runs when the cutoff write actually succeeded; on failure it reports `false` directly. Implementing that safely needed a small refactor: `_mergeAndPersist()`'s retry loop was pulled out into a separate `_mergeAndPersistCore()` that doesn't chain onto `_writeQueue` itself, since calling the queued `_mergeAndPersist()` wrapper reactively from inside `_clearPersisted()`'s own already-queued callback would have read `_writeQueue`'s value at whatever point that callback happened to run (by then possibly already advanced by other operations queued after it), risking a circular wait between the two steps; `_clearPersisted()` now calls the unchained core directly within its own single queued step instead. An eleventh round then caught a third variant of the same "corrupt buffer wedges retries forever" failure mode from two rounds ago, one layer deeper: even a syntactically valid array can contain a malformed *element* (`null`, a stray primitive, an object with a non-string `ts`, however it got there), which reached the `e.ts > clearedAt` comparison during filtering and threw there instead, with the same indefinite-retry consequence as an outright parse failure or a non-array top level. `parseBuffer()` now filters out anything that isn't a well-formed entry (an object with a string `ts`) after parsing, closing this specific class of corruption at the element level instead of only the top level. A twelfth round then caught that `_clearPersisted()`'s now-genuine `true`/`false` result (from the tenth round above) still never left `gsUtils.js`: `clearLogBuffer()` discarded it, `background.js`'s `clearLogs` case never set a response, and the debug page's "Clear log" button always just refreshed the log view regardless, so a failed clear (a transient storage error, or exhausted purge retries) looked identical to a successful one, with no indication the old entries were still there. `clearLogBuffer()` now returns the result instead of discarding it, `background.js` responds with `{ success }`, and "Clear log" briefly shows "clear failed" on the button (mirroring "repair favicons now"'s existing result-text pattern) instead of silently refreshing as if nothing were wrong. A thirteenth round then caught a gap in the sixth round's own async-IIFE fix: if any awaited call inside the switch threw (`performTabChecks()` failing, an unexpected manual-backup error, etc.), the detached IIFE simply rejected with nothing catching it, so `sendResponse()` never ran — since the outer listener had already returned `true` to keep the channel open, the sender was left waiting until the message port itself eventually tore down instead of promptly seeing the failure. The switch is now wrapped in `try`/`catch`, logging the error and still calling `sendResponse()` so the channel is always settled either way.
- **Battery-specific timeout (and the older "never suspend while charging" option) never actually reacted to unplugging the computer** (`background.js`, `manifest.json`, `offscreen.html`, `offscreen.js`, #252): `navigator.getBattery()` is a Window-only API and was never actually available in this MV3 service worker (a `@TODO` comment already flagged this back when "never suspend while charging" first shipped, but the surrounding code was left in place anyway), so `tgs.setCharging()` was never called and `tgs.isCharging()` always returned `undefined`. Confirmed via live testing: unplugging never updated any tab's timer, and a newly opened tab always used the AC timeout even while genuinely running on battery; the debug page's own power-source indicator (added above) still showed the correct state, since it reads `navigator.getBattery()` directly in its own page context, which is exactly what pointed at the service worker being the one with no access. Fixed properly with `chrome.offscreen`: a new hidden offscreen document (`offscreen.html`/`offscreen.js`, real Window context) reads the battery and reports charging-state changes to the service worker via a `batteryStatus` runtime message, handled in `background.js`'s existing message listener the same way the old (non-functional) in-service-worker listener tried to. `ensureOffscreenDocument()` (re)creates the document on every service worker wake if missing, since it isn't guaranteed to survive across recycles. Requires the new `offscreen` permission (silent, no runtime consent prompt or Chrome Web Store warning, unlike `downloads`/`identity`).
- **"Reload also unsuspends tabs in the background" never fired when combined with "Discard tab after suspend"** (`suspended.js`, #158): `initTab()`'s `quickInit` fast path (used for any background tab suspended while "Discard after suspend" is on) returns before ever reaching `setUnloadTabHandler`, so the `beforeunload` listener the new option depends on was never registered for those tabs; the option looked enabled but silently did nothing. `quickInit` now still registers the listener when the option is on, skipping the rest of the (unneeded, since the tab gets discarded anyway) heavy setup.
- **Suspended tabs could register a second `beforeunload` listener with a stale snapshot, racing the first on reload** (`suspended.js`, #158): `initTab()` calls `setUnloadTabHandler(tab)` on every non-quick reinit, including the one `checkQueue` triggers when it finds a suspended tab unresponsive, with no guard against calling it more than once per page. A tab reinitialised this way ended up with two `beforeunload` listeners, each closing over its own `tab` snapshot; on the actual unload both fired and wrote `STATE_UNLOADED_URL` asynchronously, so whichever storage write resolved last won, independent of which snapshot was actually correct. Surfaced by testing the new background-tab reload-to-unsuspend option above: out of 4 tabs reloaded together, the 2 that had been reinitialised by `checkQueue` failed to unsuspend while the other 2 worked, non-deterministically. `setUnloadTabHandler` now registers the listener only once per page, since the suspended.html URL is stable for the tab's whole suspended lifetime, so the first capture was always the right one.
- **Multi-tab reload could still unsuspend only a random subset of the tabs, non-deterministically** (`suspended.js`, #158): even after the dedupe fix above, the `beforeunload` handler still made ~4 sequential async `chrome.storage` round trips (a diagnostic log read, a settings lookup, then a read-modify-write for the actual flag) before writing `STATE_UNLOADED_URL`, and Chrome gives `beforeunload` no guarantee that pending async work finishes before the page is torn down. Reloading several suspended tabs together contends for the same storage IPC, so on a loaded system some of those writes lost the race and never landed, and which tabs lost was different every run. `reloadUnsuspendBackground` is now resolved once at page-init time (no time pressure there) and passed in, cutting the handler's pre-write hops from ~4 down to 1.
- **Changelog modal failed silently, with no way to diagnose why** (`gsChangelog.js`): `renderVersionChangelog()` returned `false` on every failure path (bad fetch, missing version section, exception) without ever logging anything, and `options.js` marks the version "seen" unconditionally right after calling it, so a failure permanently burns the one chance to show that version's modal, with zero trace even in a `captureLogs` report. Root-caused on a real Windows "Load unpacked" checkout: `src/CHANGELOG.md` is tracked as a symlink to the root `CHANGELOG.md`, but Git for Windows without symlink support checks it out as a 15-byte text file containing just the literal target path (`../CHANGELOG.md`) instead of a real symlink, so the fetch "succeeded" with 15 bytes of garbage, found no `## [version]` heading, and gave up without a word. Every failure path now logs a `gsUtils.warning()` with enough detail to diagnose it (HTTP status, missing section name, or, for suspiciously short responses, an explicit note that this is the known broken-symlink-on-Windows scenario, not a real changelog).
- **Debug page: live log times shown in UTC instead of the viewer's local time** (`debug.js`): `entry.ts` is stored as UTC (`new Date().toISOString()`, unambiguous no matter who reads the raw history), but the live view rendered it with a raw `substring()`, so a tester's local clock and the log timestamps could be hours apart with no indication either was UTC. The live view now renders local time; the Copy/Download report keeps the raw UTC timestamps, where lack of ambiguity across machines/timezones matters more than at-a-glance readability.
- **A tab requiring several requeues in `checkQueue` (or the suspension queue) could be killed by a stale job timeout mid-progress** (`gsTabQueue.js`): `processTab()` starts a job's timeout timer only once, on its very first execution attempt (`if (!tabDetails.hasOwnProperty('timeoutTimer'))`); `requeueTab()`, called for perfectly normal reasons like "tab still loading" or "no context yet", never touched that timer. So a job needing several requeues (common when many suspended tabs get reinitialised together, e.g. after a bulk reload) kept counting down against its original attempt's deadline, and could get killed with a false "timeout" partway through, even though each individual step was fast and it was actively making progress. Confirmed via a debug report: a burst of `checkQueue ... timeout` failures across many unrelated tabs landed almost exactly 60s (the default `jobTimeout`) after the matching burst of `initialiseSuspendedTab` calls that had queued them. `requeueTab()` now clears the stale timer so the next attempt starts a fresh timeout window, since a requeue is progress, not a hang. Resetting the timer on every requeue also removed the queue's only terminal deadline, though; a tab stuck permanently requeuing (e.g. `status` never leaving `'loading'`) would no longer time out at all, which during startup could keep `performInitialisationTabChecks()`'s `Promise.all` waiting forever, so a `MAX_REQUEUES` cap (100) now forces those through the existing exception path instead.
- **Suspended tabs could get stuck showing the raw `suspended.html#ttl=…&uri=…` placeholder, no title, no favicon, until manually reloaded** (`tgs.js`, `suspended.js`): right after a suspended tab's status flipped to `complete`, the background sent it an `initTab` message, but that page's own script (and therefore its `chrome.runtime.onMessage` listener) could still be a beat behind, especially with many suspended tabs completing in the same burst (e.g. dozens of tabs restored at browser startup). A single failed send meant `initTab()` never ran at all for that page load, so nothing (title, favicon, or the page being shown) ever got set. The existing background responsiveness check would eventually catch and reinitialise it, but that recovery pass is `setTimeout`-based and not guaranteed to survive a service worker recycle in between, so in practice a tab could stay stuck well past 10s, if it recovered at all, until manually reloaded. Two changes: the `initTab` send now retries a few times with a short delay before giving up, and `suspended.js` registers its message listener as soon as the DOM is ready instead of waiting on the full localisation chain (locale storage read, possible locale-file fetch, mascot/theme application), since that chain has no fixed upper bound under the same concurrent-startup load and could still outlast a handful of fixed retries. Still reproducible under real stress-testing (hundreds of tabs, browser-crash recovery) even with those two fixes, since the original 3×150ms=450ms retry budget wasn't generous enough for every tab in a large-enough burst. `sendInitTabMessageWithRetry()` now uses exponential backoff (`[100, 200, 400, 800, 1500, 3000]`ms, ~6s total) instead of a short fixed interval: the happy path (first attempt succeeds, the overwhelming majority of tabs) is unaffected, since added time is only ever spent on retries that have already failed once, invisibly, in the background.
- **Suspended-tab page too bright in dark mode: title/URL text and mascot artwork** (`suspended.css`, `suspended.js`, #446): reported via a comment on the KB blog post about the classic TGS artwork option, then confirmed independently by another user via email. `.gsTopBarTitle`/`.gsTopBarUrl` used `color: inherit` (full-brightness `--foreground`) since the visual redesign, noticeably brighter than before at night; now dimmed to `--muted-foreground` in dark mode only, matching the `.watermark` text that was already using it. Separately, the suspended-page mascot (`snoozy_tab`/`snoozy_tab_awake`) had no dark-mode treatment, unlike the favicon which already gets a `filter: invert(1) grayscale(1)` low-contrast pass via `.faviconWrapLowContrast`; the mascot now gets its own dimming filter (`.mascotLowContrast`, `brightness(0.7)`), toggled alongside theme/mascot updates in dark mode regardless of whether the classic TGS artwork option is on; the first pass only dimmed the legacy artwork, but the default modern mascot glares just as much and is what most users actually see.
- **Downloading a single Drive backup from the Backup page nested it inside a `tms-backups` subfolder** (`gsBackup.js`): the "Download" button next to each Drive backup entry in `backup.html` (`downloadDriveBackupAsFile`) reused `BACKUP_SUBDIR`, the same `tms-backups/` prefix used by the automatic/manual local-backup rotation system (`performLocalBackup`), but this is a one-off explicit download of a specific file, not part of that rotation (it's never added to `tmsLocalBackupIds`, so it's not tracked or cleaned up either). It now downloads straight into the browser's default Downloads location like any other single-file download, instead of silently nesting it in a subfolder meant for a different feature. Also switched `conflictAction` from `overwrite` to `uniquify`: safe inside the extension's own isolated subfolder, but risked silently clobbering an unrelated same-named file now that the download lands in the general Downloads root.
- **Legacy mascot artwork used the raw original TGS assets instead of TMS's own modified versions** (`img/legacy/`): the "Use the classic The Great Suspender artwork" option (added in 9.0.2) had every `img/legacy/` asset sourced from plain upstream The Great Suspender artwork rather than TMS 8.1.6's own re-branded set (the Marvellous "M" badge on the mascot's chest, TMS's reworked `snoozy_tab`/`snoozy_tab_awake` illustrations, etc.), most visible on the SVGs, where the wrong ~2.6 KB "_original" TGS file had been used instead of TMS's ~32 KB reworked one. Re-sourced every asset from TMS 8.1.6 (`chromeDefaultFavicon(Sml)`, `chromeDevDefaultFavicon(Sml)`, `ic_suspendy_*`, `suspendy-guy*`, `snoozy_tab*`): toolbar/action PNG icons copied as-is, illustrations re-converted to WebP (`cwebp -q 90 -m 6`), SVGs copied as-is. No code changes; `gsMascot.js`'s mapping was already correct, only the asset content was wrong.
- **"Suspend/unsuspend all tabs in this group" ignored whitelist, pinned, audible, form-input, and active-tab protections** (`tgs.js`, #133): `suspendTabGroup()` queued every tab in the clicked group with `forceLevel` 1, the same level used for an explicit single-tab "force suspend" action, which skips every protection check above `isSpecialTab()`. Since the user only actually acted on one tab in the group, the other tabs swept along with it (the active tab, a pinned tab, one mid-form-input, etc.) could be suspended without any of the protections the equivalent single-tab/selected-tabs actions respect. Now queued at `forceLevel` 2, the same level `checkTabEligibilityForSuspension()` uses for a soft/normal suspend, so those protections apply to the tabs the user didn't directly click on. Found via Codex code review. A follow-up review round caught that this fix went too far: `forceLevel` 2 unconditionally rejects the currently focused tab, so if the acted-on tab (the one clicked, or the active tab when triggered via keyboard shortcut) was itself the active tab, the group action could no longer suspend it at all — the very tab the user meant to act on. The acted-on tab now stays at `forceLevel` 1 while the rest of the group uses `forceLevel` 2, matching how the equivalent single-tab actions already treat the tab the user directly triggers versus tabs only swept along incidentally.
- **Battery-specific auto-suspend timeout stopped working on Chrome versions below 150** (`background.js`, #252): `ensureOffscreenDocument()` called `chrome.offscreen.hasDocument()` unconditionally, but that method only exists from Chrome 150 onward, while `manifest.json` still declares Chrome 110 as the minimum; on 110-149 the call threw before `createDocument()` ever ran, so the offscreen document (and therefore battery status and the timeout override) silently never worked at all on any version below 150. Added a `hasDocument` feature-check with a `self.clients.matchAll()` fallback, which is a standard `ServiceWorkerGlobalScope` API available across the whole supported range, plus a `try`/`catch` around the whole function so a future API surface change fails closed instead of throwing unhandled. Found via Codex code review.
- **"Repair favicons now" (and other async debug/settings actions) could report failure even after completing successfully** (`background.js`, #449): `messageRequestListener` was declared `async`, so it always returned a `Promise` from `chrome.runtime.onMessage.addListener`'s perspective instead of the literal `true` Chrome requires to keep a message channel open for a later `sendResponse()`; any case that `await`ed something before responding (`repairFavicons` in particular, since it awaits `gsSession.performTabChecks()` and returns real counts) could have its response silently dropped once the channel closed early. The listener is now a synchronous function that runs its body in an internal async IIFE and returns `true` immediately, so the channel stays open for the actual `sendResponse()` call regardless of how long the awaited work takes. Found via Codex code review.
- **Battery-specific timeout became impossible to configure once the AC timeout was set to "Never"** (`options.html`, #252): the new battery-timeout row shared the `.autoSuspendOption` class with the AC-timeout-dependent options, so `setAutoSuspendOptionsVisibility(false)` (triggered whenever AC timeout = "Never") hid it along with them — even though `checkTabEligibilityForSuspension()`'s effective-timeout logic explicitly supports a battery timeout overriding a disabled AC timeout. The row no longer carries that class, so it stays visible and configurable independently of the AC timeout's value. Found via Codex code review.
- **"Reload also unsuspends tabs in the background" didn't take effect on already-suspended tabs when toggled** (`suspended.js`, #158): `setUnloadTabHandler()` closed over the option's value as it stood at the tab's own `initTab()` time; toggling the option afterward had no effect on tabs already suspended and loaded, since nothing about the option change ever reached those pages. The captured value is now mutable module state, kept in sync via a `chrome.storage.onChanged` listener on the `gsSettings` blob gsStorage persists all options under, so a toggle in Options applies immediately to every open suspended page without adding any `chrome.storage` read inside the time-sensitive `beforeunload` handler itself. Also fixed the same closure-over-initial-value gap in `initTab()`'s `quickInit` fast path, which previously only registered the listener at all when the option was already on at that moment, so enabling it later never added a handler for tabs that took that path. Found via Codex code review.

## [9.0.2] — 2026-08-06

### Added
- **"Clear favicon cache" debug action** (`debug.html`, `debug.js`, `gsIndexedDb.js`, #395): the debug page can now wipe every cached favicon (`gsFaviconMeta` in IndexedDB) in one click. Favicons are rebuilt from Chrome's own cache the next time each tab is suspended, useful when a suspended tab keeps showing a stale or wrong icon after the site's own favicon has changed.
- **About page: debug link highlighted in its own callout box** (`about.html`, `style.css`, `icons.svg`): the debugging link was a plain paragraph, easy to miss among the other links. Gave it a bordered callout with a wrench icon, matching the card/box language used elsewhere in the extension's UI.
- **"Use the classic The Great Suspender artwork" option** (`gsMascot.js`, `gsStorage.js`, `options.html`, `options.js`, `tgs.js`, `gsFavicon.js`, `gsUtils.js`, `suspended.js`, `updated.js`, `background.js`, all 18 `messages.json` locales, `img/legacy/`): new checkbox in Options → General swaps the current robot mascot/icon set back to the original The Great Suspender artwork the user supplied (converted to WEBP where the current asset is also WEBP; toolbar/action icons stay PNG since `chrome.action.setIcon` requires raster). A single mapping in the new `gsMascot` module resolves every icon/illustration path app-wide (all HTML pages' `<img>`/`<link rel="icon">` via the shared `documentReadyAndLocalisedAsPromised` bootstrap, the toolbar icon, the suspended-tab favicon fallback list); the only image with no legacy equivalent (`suspendy-guy-lotus.webp`) keeps using the new artwork. Also gave every extension page (options, about, backup, etc.) an explicit `<link rel="icon">`, they had none before and relied on the static manifest icon, which is why the toolbar/tab icon used to snap back to the new mascot on navigation between TMS's own pages; `tgs.refreshDefaultIcon()` now also keeps the global default toolbar icon (used by any tab without its own per-tab icon override, e.g. TMS's own pages) in sync with the setting.
- **Bulk-delete Drive backups per device** (`backup.html`, `backup.js`): the "Drive backup files" list already groups files by device when a Drive account has backups from more than one machine, but there was no way to clear out one machine's files in one go, only one-by-one. Added a "Delete all" button next to each device's group heading; confirms via a modal (device name + file count) before deleting, same pattern as the existing single-file delete confirmation. Partial failures (some files fail to delete) leave the group in place with an inline error instead of silently succeeding.
- **Test/force-suspend buttons for the "Always suspend" list** (`options.html`, `options.js`, `tgs.js`, `background.js`), mirroring the whitelist's test/wake pair: "Test list" opens the same kind of modal as the whitelist test (open tabs currently matching the list, click to jump to one), and "Suspend matching tabs now" force-suspends every open tab matching the list immediately, instead of waiting for the normal timeout, the inverse of the whitelist's "Wake whitelisted tabs".
- **"Always suspend" list, to accompany the whitelist** (`options.html`, `gsStorage.js`, `gsUtils.js`, `gsTabSuspendManager.js`, #103): a new list (Settings page, next to the whitelist) of URLs that always suspend after the normal timeout, bypassing the pinned/audible/form-input protections that would otherwise keep them open indefinitely (e.g. a background Twitch/YouTube tab left playing audio). Same matching rules as the whitelist (plain text or `/regex/`). An explicit per-tab manual pause and global protections (offline, charging, "never suspend") are still respected, this only overrides the passive/automatic ones. Named to avoid the "whitelist"/"blacklist" polarity ambiguity flagged in the issue thread, not exposed anywhere as "blacklist".

### Changed
- **"Whitelist" reworded to "allowlist" in user-facing strings** (all 18 `messages.json` locales): "lista bianca"/"whitelist" wording in tooltips and popup messages replaced with "elenco consentiti"/"allowlist" (and each locale's own equivalent). Internal code (the `WHITELIST` storage key, `checkWhiteList()`, HTML ids like `whitelistLbl`/`testWhitelistBtn`) intentionally left untouched; this was a user-facing copy pass only, not a rename of the underlying feature.
- **"Wake allowlisted tabs" reworded to match the always-suspend list's "now" phrasing** (`html_options_whitelist_unsuspend`, `html_popup_unsuspend_whitelisted_tabs`, all 18 locales): "Wake allowlisted tabs" / "Riattiva le schede nell'elenco consentiti" replaced with "Wake matching tabs now" / "Riattiva subito le schede corrispondenti", mirroring the always-suspend list's "Suspend matching tabs now" so the two paired buttons read as a matched set.
- **Backup page: smaller font for the Drive account email and connection-method lines** (`backup.css`): `.driveUserEmail` (shared by both, since the connection-method line reuses that class) dropped from `--text-sm` to `--text-xs`, they're secondary detail, not the primary account/folder label.
- **Extension description reworded** (`ext_extension_description`, all 18 `messages.json` locales): the manifest/store description ("Make your computer run smoothly by suspending the tabs you aren't using") had drifted from the tone used everywhere else TMS is described (website, README). Replaced with "Free your memory. Suspend the tabs you don't need, right now." / "Libera la memoria. Sospendi subito le schede che non ti servono.", matching the README tagline ("Free your memory. Suspend what you don't need."); the other 16 locales translated by hand since `check-locales.js` doesn't flag stale-but-still-translated strings, only missing or byte-identical-to-English ones.

### Fixed
- **`captureLogs` went silently blind after the Service Worker's first restart of a session** (`background.js`): the persisted `chrome.storage.local` flag was only restored inside `startupOnce()`, which runs once per browser session, not once per Service Worker spawn, since the in-memory flag resets to `false` on every SW recycle (routine in MV3), captureLogs stopped working after the first restart, undermining the debug reports it's meant to support. Moved the restore to unconditional top-level code so it runs on every wake. Also dropped a dead call to a since-removed `tgs.updateTabIdReferences` in the `onReplaced` listener.
- **Favicon data URLs bloated the debug log buffer** (`gsUtils.js`): tab dumps logged via `captureLogs` (`checkQueue`'s "Updated tab" entries) serialized the whole tab object including `favIconUrl` as a base64 `data:` URL, often several KB of text per entry, evicting most of the 500-entry log buffer within seconds on tab-heavy sessions. Replaced with a length + short hash fingerprint so favicon changes between log lines are still comparable without the bloat.
- **Popup's "Backup now" bypassed the 30s manual-backup cooldown** (`gsBackup.js`, `background.js`, `backup.js`, `popup.js`): the cooldown after a manual backup was only ever a per-page UI debounce local to `backup.html`'s own button; the popup's own "Backup now" menu item had no cooldown at all and didn't share state with the page, so triggering from both let a user fire off backups back-to-back with no pause. Moved the cooldown into `gsBackup.performManualBackup()`, tracked in `chrome.storage.session` so it's enforced regardless of which UI triggers it; `backup.html` now also checks for a cooldown already in progress (e.g. started from the popup moments earlier) when the page loads, and the popup disables its "Backup now" item and shows a live countdown if opened during one. The scheduled auto-backup alarm still calls `performBackup()` directly and is unaffected.
- **Suspended tabs kept the default TMS favicon after a browser restart until clicked** (`background.js`, #397): the startup checks that repair favicons/titles on every suspended tab (`gsSession.runStartupChecks`) only ran from the service worker `activate` event (install/update only) and `chrome.runtime.onStartup`, which some Chromium builds (notably Brave) never fire after a normal restart, leaving suspended tabs stuck with the generic icon until manually focused. Added a fallback that checks a `chrome.storage.session` sentinel (cleared at the browser-session boundary) on every service worker wake and runs the startup checks once if it's missing, regardless of whether `onStartup` fired. Thanks @pbc-commits for the investigation and root-cause writeup.
- **"Claim by default" option unclear even to technical users** (`options.html`, #151): the checkbox crammed its explanation into a long parenthetical in the label itself. Shortened the label and moved the explanation into a proper tooltip (same pattern already used elsewhere on the page): it takes over tabs already suspended by another compatible extension (an old TGS install, or a different TMS install) by rewriting their `suspended.html` URL to this extension. Translated across all 16 non-English/Italian locales.
- **"Allow access to file URLs" toggle missing on every Chromium browser** (`manifest.json`, #393, #416, #267): Chrome only shows this toggle on an extension's details page when the manifest declares `file://` host access; TMS's `host_permissions` only ever listed `http://*/*`/`https://*/*`, so the toggle was invisible everywhere (confirmed on both fresh Chrome and Brave installs, not a Brave-specific quirk as first assumed). Added `"file:///*"` to a new `optional_host_permissions`, same on-demand pattern as `downloads`/`identity`, no access is granted until the user explicitly flips the toggle.
- **Google Drive "Connect account" fails or hangs on Brave and Vivaldi** (`gsBackup.js`, #420): `chrome.identity.getAuthToken()` doesn't work on these Chromium forks, for two different reasons confirmed by manual testing. **Brave** rejects it outright: it attaches a custom URI scheme to the OAuth request that Google's backend no longer accepts (brave/brave-browser#38066, open since May 2024, unassigned, no fix timeline), and Brave's own implementation additionally opens a visible native tab that hits Google's servers and shows the raw `Error 400: invalid_request` page before failing, not something extension code can suppress. **Vivaldi** disables Google's browser sign-in entirely for privacy reasons, and its `getAuthToken()` callback simply never fires in that state (`"The user turned off browser signin"` only ever appears as an unread `chrome.runtime.lastError` in the console), silently hanging forever with no UI at all. Fixed by adding `chrome.identity.launchWebAuthFlow()` as a fallback, used as follows without changing anything for installs where `getAuthToken()` already works: `navigator.brave.isBrave()` is checked once up front to skip straight to the fallback on Brave (avoiding that doomed first attempt and its ugly tab), `getAuthTokenViaChromeIdentity()` is now raced against a timeout (6s for silent/background calls, which should never show any UI at all; 45s for the interactive "Connect" click, generous enough for a real consent flow) so a hung callback like Vivaldi's still surfaces as a failure instead of hanging forever, and any other failure from an interactive click falls back to `launchWebAuthFlow()` too, covering any other Chromium fork with a similar issue we haven't hit yet. Once the fallback succeeds for an install, that's remembered (`chrome.storage.local`) so later calls (including background auto-backup) go straight to it instead of re-probing a method already known to fail. Fallback tokens are cached in `chrome.storage.session` with their expiry and silently refreshed when possible. Requires a second OAuth client in Google Cloud Console, type "Web application", with `https://<extension-id>.chromiumapp.org/` registered as an authorized redirect URI (the existing "Chrome App" client keeps serving `getAuthToken()` unchanged). The Backup page now also prints which method is active as a second line under the "TMS Backups" folder name (e.g. "Chrome Identity API (getAuthToken)"), not translated and not a separate row, same treatment as the folder name itself, just a quick way for support to tell which OAuth path an install is on.
- **Clicking a suspended page didn't restore it until the shortcut-key label appeared** (`suspended.js`, #320): `initTab()` called `showContents()` (which makes the page visible and interactive) only after `await tgs.getSuspensionToggleHotkey()`, a `chrome.commands.getAll()` call that can be slow. The unsuspend click handlers were already attached earlier, so the page just sat hidden/inert for no reason while waiting on a purely cosmetic hotkey label. Moved `showContents()` before that call; the hotkey label now fills in after the page is already interactive instead of gating it.
- **`options.html` "Claim by default" tooltip line break**: missing blank line between the two tooltip messages made them render as one run-on paragraph instead of two, unlike every other multi-topic tooltip on the page (e.g. "Always suspend").
- **"Always suspend" textarea showing literal `undefined`** (`gsStorage.js`): `getSettings()` only applied defaults on a brand-new install, so users who already had settings saved before the "Always suspend" list was added never got that key backfilled; `gsAlwaysSuspendList` stayed `undefined`, and `options.js` wrote it straight into the textarea's `.value`. `getSettings()` now backfills any missing/null keys against `getSettingsDefaults()` on every load, matching the fallback `getOption()` already had.
- **Inconsistent tooltip line-wrapping across `options.html`/`backup.html`** (all 18 `messages.json` locales): several tooltips ("Claim by default", "Always suspend", "Append URL to title", "Enable backup", "News feed") crammed long sentences onto a single unwrapped line instead of following the `data-i18n-tooltip` convention already used elsewhere on the page (e.g. "Enable context menu": one short line per paragraph, blank line between paragraphs). Split the long lines into `line1a`/`line1b`/... sub-keys to match, translated by hand into all 16 non-English locales instead of waiting on Crowdin (deviation from the usual EN/IT-only rule, done explicitly for this one change). `fr-FR` mirrors `fr` as usual. `npm run check-locales` reports all locales in sync.
- **"Claim by default" label too vague** (`en`/`it` `messages.json`, #151 follow-up): shortening the label to just "Claim by default" / "Rivendica per impostazione predefinita" left it meaningless without opening the tooltip. Reworded to "Claim tabs suspended by other extensions" / "Rivendica schede sospese da altre estensioni", still one line, but says what it does without requiring a hover.

### Localization
- **Filled in translations for all 15 non-English/Italian locales** (`src/_locales/*/messages.json`): after merging PR #413, `check-locales.js` still flagged ~30-38 keys per locale as untranslated, the 7 strings reverted by the `a2e168f` source edit plus the backup/permissions/news-feed strings introduced by the same commit. Translated all of them directly instead of waiting on the next Crowdin sync, reusing the pre-`a2e168f` Crowdin translations (with punctuation adjusted to match the new source) where available. `fr-FR` (a stale duplicate of `fr`, not a genuinely distinct regional variant) was brought back in line with `fr`. A handful of short strings (`min`, `Version`, `Tab`/`Tabs`, `Local`, `Folder`, `System`, `page`) are legitimate loanwords spelled the same as the English source in specific locales (German, Spanish, French, Portuguese, Indonesian, Czech, Slovak), added to a `KNOWN_COGNATES` allowlist in the script so it stops flagging them as gaps. `npm run check-locales` now reports all 16 locales in sync.
- **Added `scripts/check-locales.js`** (`npm run check-locales`): compares every non-English/Italian locale against `en/messages.json` and reports keys that are missing or byte-identical to the English source (untranslated, or a Crowdin fuzzy-match broken by a source string edit). Added after PR #413 ("New Crowdin updates") appeared to revert 7 already-translated strings back to English across all 15 locales, turned out not to be a regression: commit `a2e168f` swapped an em dash for a colon/comma in those 7 English source strings, which broke Crowdin's fuzzy match and made it fall back to the English source on the next sync, on top of the ~16 genuinely new keys from the same commit still awaiting translation. The script makes this kind of source-edit fallout visible locally instead of only showing up as a surprising diff in the next Crowdin PR.

## [9.0.1] — 2026-08-04

### Added
- **Backup activation nudge** (`gsStorage.js`, `gsBackup.js`, `background.js`, `popup.html`, `popup.js`, `backup.html`, `backup.js`, `en/messages.json`, `it/messages.json`): users who have never enabled automatic backup (`AUTO_BACKUP_ENABLED = false`, the default) now see a quiet amber badge on the extension icon plus a banner in the popup and a note on the backup page, encouraging them to turn it on, prompted by issue #410, where a Chrome-side "corrupted extension" repair flow wiped a user's sessions with no backup in place. Users can snooze the nudge for 10 days (`BACKUP_NUDGE_DISMISSED_UNTIL`) or permanently opt out via a checkbox on the backup page (`BACKUP_NUDGE_OPTOUT`); there is no repeated popup/alert, only the persistent badge/banner/note.
- **On-demand `downloads`/`identity` permissions and backup transparency** (`manifest.json`, `gsStorage.js`, `gsBackup.js`, `gsNewsFeed.js`, `backup.html`, `backup.js`, `options.html`, `options.js`, `en/messages.json`, `it/messages.json`): responding to issue #411, `downloads` and `identity` moved from unconditional `permissions` to `optional_permissions`, Chrome now only prompts for them when a user actually enables local backup (`chrome.permissions.request({permissions:['downloads']})` on the auto-backup checkbox) or connects Google Drive (`identity`, on the "Connetti Drive" button). Users who already had these granted under 9.0.0 see no re-prompt (`chrome.permissions.request` is a silent no-op when already granted, and Chrome preserves already-granted permissions across a required→optional manifest change). Also added: an opt-out for the daily news-feed request (`gsNewsFeedEnabled`, default on), a one-time in-app banner plus a persistent reference note pointing to the permissions documentation (`options.html`/`backup.html`), and a note explaining (plus automatic cleanup of) the local safety-net file `performEmergencyBackup` still writes when the configured destination is Drive-only.

## [9.0.0] — 2026-06-24 (session backup)

### Added
- **Emergency backup on browser close** (`gsBackup.js`, `background.js`): `chrome.runtime.onSuspend` now triggers `performEmergencyBackup()`, which writes a local session backup (when auto-backup is enabled) right before the browser shuts down, instead of relying solely on the next scheduled `tms-auto-backup` alarm. When the configured destination is Google Drive, the upload itself is not attempted at shutdown (too slow/risky for the shutdown window); the session is queued in `chrome.storage.local` (`tmsPendingDriveBackup`) and uploaded via `retryPendingDriveBackup()` on the next `chrome.runtime.onStartup`, retrying with backoff (30s/2min/10min) through a new `tms-pending-backup-retry` alarm if the network isn't ready yet. Auth errors are treated as terminal (flagged via the existing Drive-auth-error badge) rather than retried.
- **Backup flood protection** (`backup.js`, `en/messages.json`, `it/messages.json`): the "Backup now" button (session backup, local or Drive) and the "Save settings to Drive" button are now disabled immediately on click and remain disabled for 30 seconds after the operation completes. The status bar shows a live countdown ("Wait Xs before the next backup.") so the user knows when the button will be re-enabled. This prevents accidental back-to-back backup spam.
- **Tab Groups health check** (`health.js`, `en/messages.json`, `it/messages.json`): the Tab Health page now detects grouped tabs in broken state after browser restart, both the Chrome/Edge path (discarded+grouped+suspended tabs) and the Brave path (grouped tabs showing `chrome://newtab/`). When broken tabs are found, a "Repair grouped tab(s)" button appears as the highest-priority action and runs the recovery logic (re-create in-place with the correct URL and restore group membership). If no broken tabs exist the scan shows `0 ✓`. The portable recovery logic is ported from `gsSession.recoverChromeDiscardedGroupedTabs` and `gsSession.recoverBraveGroupedTabs`.
- **Whitelist test: styled modal instead of a browser alert()** (`options.html`, `options.js`, `style.css`, `en/messages.json`, `it/messages.json`): the "Test whitelist" button on the options page used to show matching open tabs as a plain-text native `alert()`. It now opens a styled modal (matching the existing Drive-import confirmation modal's look) listing each matching tab as a clickable link, clicking one switches focus to that tab. No artificial 22-item cap anymore; the list scrolls instead. Empty state ("no matches") is now shown inside the same modal instead of a separate `alert()`.
- **Option to preserve YouTube playback position on suspend** (`gsStorage.js`, `gsTabSuspendManager.js`, `options.html`, `options.js`, `en/messages.json`; PR #404 by @iGhost): new `gsAddYouTubeTimestamp` setting, defaulting to `true` to preserve existing behavior, when disabled, suspending a YouTube tab no longer appends the playback timestamp to the suspended URL. Solves #284. `generateUrlWithYouTubeTimestamp` also refactored from a callback-wrapped Promise to async/await.
- **News feed** (`news.html`, `news.js`, `gsNewsFeed.js`, `gsNavBadge.js`): new dedicated "News" page in the sidebar that fetches and displays the latest TMS articles from the Marvellous Codeworks blog (`kb.marvellouscode.works/blog/rss.xml`). Each card shows title, excerpt, publication date, and a link to the full article. Details:
  - Feed cached in `chrome.storage.local` (TTL 6 h); periodic refresh via `chrome.alarms` in the service worker (`tms-news-feed`, every 6 h). Opening `news.html` also triggers an immediate `fetchAndCacheIfStale()` directly from the page.
  - Only articles tagged `"The Marvellous Suspender"` via RSS `<category>` are shown; articles from other Marvellous Codeworks products (e.g. TGD) are filtered out.
  - Maximum 10 items displayed, laid out in a 2-column grid.
  - A red badge dot on the "News" sidebar entry signals unread articles; opening the page marks all items as seen. Badge logic lives in the shared `gsNavBadge.js` module, loaded by every extension page.
  - `manifest.json`: added `https://kb.marvellouscode.works` to `connect-src` (required for both extension pages and the service worker under MV3).
- New i18n keys (`html_sidebar_news`, `html_news_title`, `html_news_desc`, `html_news_loading`, `html_news_empty`) in `en` and `it` locales.

- **News feed: per-article read tracking** (`news.js`, `news.html`, `gsNewsFeed.js`, `style.css`): articles are no longer marked as read simply by opening the News page. Unread articles are highlighted with a blue left border and a "NEW" pill in the date area. Clicking the article title link marks that single article as read and removes the highlight. A "Mark all as read" link appears top-right above the grid when unread items exist; it disappears once everything is read. `gsNewsFeed.markSeen(link)` added as a new public function for single-item tracking. New i18n keys `html_news_new_pill` and `html_news_mark_all_read` in `en` and `it`.

- **News feed alarm: per-install jitter within working hours** (`gsNewsFeed.js`): reduced to once per day (`periodInMinutes: 1440`, `CACHE_TTL_MS: 24h`). The fetch time is random but fixed per installation, generated once within the 08:00–20:00 local window and persisted in `tmsNewsFeedMinuteOffset`. Spreads ~100k users across 720 minutes (~139 req/min), avoiding coordinated bursts on the GitHub Pages CDN. The debug page shows the assigned daily time (e.g. "daily at 14:23 local").

- **Debug: news feed card** (`debug.html`, `debug.js`): new card in the Controls section of the Debugger page. Shows last fetch timestamp, unread/total article count, next scheduled alarm time, and per-install jitter slot (e.g. "daily at 14:23 local"). The "force refresh" link bypasses the TTL and triggers an immediate RSS fetch; it is hidden on Chrome Web Store installs (`update_url` check) and visible only on unpacked developer builds. Action links ("run claim", "force refresh") are positioned top-right in the card header, matching the style of the other debug controls.
- **RSS icon** (`img/icons.svg`): added `#rss` symbol (Lucide, three arcs + dot) to the shared sprite so it can be referenced as `<use href="img/icons.svg#rss"/>` from any extension page.

- **Donate bar in sidebar** (`gsDonateLinks.js`, `style.css`): a compact row of 3 favicon-sized icons (GitHub Sponsors, Ko-fi, Buy Me a Coffee) now appears at the bottom of the left navigation sidebar on every extension page. Icons are bundled locally (`img/favicon-github.png`, `img/favicon-kofi.ico`, `img/favicon-buymeacoffee.ico`) to comply with the extension's `img-src 'self'` CSP. Rendered at 16 px with 45 % default opacity, full opacity on hover. Accessible via `title` and `aria-label` attributes.

- **Support development section** (`about.html`): added a "Support development" section to the About page, placed between the issue reporting/debugging links and the Credits block. Lists three donation platforms (GitHub Sponsors, Ko-fi, and Buy Me a Coffee) with a brief introductory sentence. New i18n keys `html_about_section_support` and `html_about_support_text` added to `en` and `it` locales.
- **Settings backup/restore cards** (`backup.html`, `backup.js`, `gsBackup.js`): the flat "Settings backup" section is redesigned with two cards matching the session-restore layout. The **Local card** always shows Export and Import buttons. The **Google Drive card** appears only when Drive is connected and offers both "Save settings to Drive" (existing) and the new **"Restore settings from Drive"** button, which downloads `tms-settings.json` from the `TMS Backups` Drive folder, validates it, and applies it after user confirmation. The settings section is moved to the bottom of the page so session backup and restore have primary prominence.
- `gsBackup.getDriveSettingsInfo()`: lightweight metadata query that returns `{ id, modifiedTime }` for `tms-settings.json` on Drive, or `null` if absent. Used to display the last-backup date in the Drive settings card.
- `gsBackup.downloadDriveSettingsContent()`: fetches `tms-settings.json` from Drive; throws `TMS_SETTINGS_NOT_FOUND` if the file does not exist. Internally shares `_findDriveSettingsFile()` with `getDriveSettingsInfo()` to avoid duplicate folder queries.
- `applySettingsJson(jsonText)` extracted as a shared internal helper in `backup.js`, used by both the local file import and the new Drive restore handler, eliminating duplication.
- **Overwrite protection for Drive settings backup** (`gsBackup.js`, `backup.js`): saving settings to Drive now has two safety layers. **Option A** (UI): before overwriting an existing `tms-settings.json`, the "Save settings to Drive" button shows a confirmation dialog with the date of the existing backup ("Existing settings backup from DD/MM/YYYY HH:MM. Overwrite?"). If the user cancels, no action is taken. **Option B** (silent): `performDriveSettingsBackup()` copies the current `tms-settings.json` to `tms-settings-prev.json` before overwriting, so the previous version is recoverable from Drive even after a mistaken save. Private helper `_writeDriveFile(token, folderId, filename, jsonString)` extracted to support both files without code duplication. After a successful save the Drive-card date badge updates immediately.
- New i18n keys (`html_backup_settings_local_title`, `html_backup_settings_drive_title`, `html_backup_settings_drive_restore`, `js_backup_settings_drive_downloading`, `js_backup_settings_drive_not_found`, `js_backup_settings_drive_restore_success`, `js_backup_settings_drive_restore_error`, `js_backup_settings_drive_overwrite_confirm`) in `en` and `it` locales.

### Changed
- **Options page: hide/show helpers unified on the `.hidden` class** (`options.js`, `style.css`): `setSyncNoteVisibility` and `setAutoSuspendOptionsVisibility` toggled `el.style.display` directly while `setForceScreenCaptureVisibility` already used the `.hidden` class. All three now toggle `.hidden`; a scoped `display: none` rule was added for these elements so they collapse (the base `.hidden` only flips `visibility`).
- **Tab Groups startup workarounds disabled for testing** (`gsSession.js`, `background.js`): commented out all Chrome/Edge/Brave Tab Groups session-restore workarounds (`handleReplacedTabs`, `recoverChromeDiscardedGroupedTabs`, `recoverBraveGroupedTabs`, `cacheGroupedSuspendedTab`, `handleGroupedTabToNewTab`, `handleLateReplacedTab`, `pushReplacedTab`) to verify whether Chrome 150 (150.0.7871.47) natively fixes the grouped-tab startup bug (crbug.com/522338670). The event-driven handlers are kept as comments for quick reactivation; the scan-based recovery is now available on demand from the health page.
- **GitHub Sponsors links** (`about.html`, `gsDonateLinks.js`): "GitHub Sponsors" is now a plain-text label followed by two separate sponsor links, `gioxx` and `rkodey`, replacing the single link that pointed only to gioxx. In `gsDonateLinks.js` the same pattern is applied via a new `groupLabel` property that renders a text span before the first item in the group.

- **Restore from backup** (`backup.html`, `backup.js`, `gsBackup.js`): new "Restore from backup" section at the bottom of the Backup page. Users can now recover a session directly from this page without manually navigating to the Sessions tab.
  - **From a local file**: a file picker loads any `tms-session-*.json` backup file, parses it, and imports it into IndexedDB as a saved session (auto-named from the filename, e.g. "Backup 2026-06-24 09:30"). A link to `history.html` appears inline so the user can immediately open Sessions to restore the desired windows.
  - **From Google Drive** (shown only when Drive is connected): "Load Drive backups" fetches the list of backup files from the `TMS Backups` Drive folder, populates a dropdown, and lets the user import the selected file with a single click. Same import-and-link flow as the local path.
- Three new public functions to support the restore flow: `gsBackup.listDriveBackups()`, `gsBackup.downloadDriveBackupContent(fileId)`, `gsBackup.importBackupJson(jsonText, sourceName)`.
- New i18n keys (`html_backup_restore_*`, `js_backup_restore_*`) in `en` and `it` locales.
- `gsUtils.localiseHtml()`: support for `data-i18n-aria-label` attribute, sets `aria-label` from the i18n message key, enabling accessible labels on form controls without hardcoded strings.

- **Multi-device Drive backup: per-device rotation and friendly names** (`gsBackup.js`, `gsStorage.js`, `backup.html`, `backup.js`, `backup.css`): Drive backups are now device-aware. Each Chrome installation generates a stable 8-char hex `deviceId` stored in `chrome.storage.local`. Backup filenames embed the deviceId (e.g. `tms-session-a3f7b2c8-2026-06-29T10-30.json`) so a session saved from a lightweight secondary device (e.g. MacBook with few tabs) can never evict backups from the primary workstation. Rotation now applies per-device: each device keeps its own independent pool of up to N recent backups. Legacy backups (files created before this change, with no deviceId in the filename) are preserved and excluded from rotation. Key details:
  - **Per-device rotation (Drive only)**: `cleanupOldDriveBackups` groups files by `deviceId` prefix and prunes each group independently. Local backups are naturally per-machine (each device has its own Downloads folder), so `cleanupOldLocalBackups` uses simple N-most-recent pruning with the original date-only filename format, no deviceId needed.
  - **Device registry**: on every Drive backup, `tms-device-{deviceId}.json` is created/updated in `appDataFolder` with `{ name, lastSeen }`. This provides a lightweight record of which devices have ever backed up to this Drive account.
  - **Configurable limit**: a new "Max backups to keep" number input (min 2, max 50, default 10) on the Backup page controls retention for both destinations. For Drive the limit applies per device; for local it applies to the total on that machine. Stored in `chrome.storage.sync` via the new `AUTO_BACKUP_MAX_FILES` key.
  - **Friendly device name** (multi-device only): when the Backup page detects that more than one device has backed up to the same Drive account, a "Device name" text field appears so the user can label the current machine (e.g. "MacBook", "PC lavoro"). The name is saved in `chrome.storage.local` and written to the device registry on the next backup. The field is hidden on single-device setups to avoid unnecessary UI clutter.
  - **Restore UI**: the Drive restore dropdown groups backups by device (using `<optgroup>` with the device friendly name as label) when multiple devices are detected; legacy backups appear in a separate "Legacy backups" group. Single-device users see a flat list unchanged from before.
  - New i18n keys (`html_backup_max_files_label`, `html_backup_device_name_label`, `html_backup_device_name_placeholder`, `html_backup_device_name_save`, `js_backup_restore_legacy_group`) in `en` and `it` locales.
  - `html_options_backup_note` updated to reflect the new filename format.

- **Append original URL to suspended tab title** (`gsStorage.js`, `options.js`, `options.html`, `suspended.js`): new option (enabled by default) that appends the original URL to `document.title` of suspended pages so Chrome's "Search Tabs" (Ctrl+Shift+A) can find them by hostname. The visual title displayed inside the suspended page is unchanged. Option is exposed as a checkbox in the "Suspended tabs" section of the Options page. Closes [#249](https://github.com/gioxx/MarvellousSuspender/issues/249).
- New i18n keys (`html_options_suspended_append_url_to_title`, `…_tooltip_line1`, `…_tooltip_line2`) in `en` and `it` locales.
- **All dynamic strings now respect TMS language setting** (`backup.js`, `options.js`, `shortcuts.js`, `suspended.js`, `tgs.js`, `gsIndexedDb.js`, `gsTabCheckManager.js`): every `chrome.i18n.getMessage()` call in page and service-worker scripts replaced with `gsUtils.getMessage()`, so all runtime strings (status messages, context-menu titles, date labels, confirmations, aria-labels, session names, suspended-tab default title) follow the locale selected in TMS rather than the browser UI locale. `gsUtils.js` fallback lines are intentionally left unchanged.
- **Drive file list: numbered entries + delete button** (`gsBackup.js`, `backup.js`, `backup.css`, `en/messages.json`, `it/messages.json`): each row in the "Drive backup files" panel now shows a sequential number (1., 2., …) before the timestamp; numbers are global across device groups. A "Delete" button (red outline, `btnDanger`) appears alongside "Download"; clicking it prompts for confirmation then calls the Drive Files API `DELETE` endpoint to remove the file permanently. On success the row is removed from the DOM without a full page reload.
- **Local backup status: last timestamp + filename instead of count** (`gsBackup.js`, `backup.js`, `en/messages.json`, `it/messages.json`): the "Archived backups: N" line is replaced with "Last backup: date · filename". `performLocalBackup` now persists both `tmsLocalLastBackup` (ISO timestamp) and `tmsLocalLastBackupFile` (bare filename) in `chrome.storage.local`. New public getter `gsBackup.getLastLocalBackupInfo()` returns both. `updateBackupMeta` reads them and formats the display using `toLocaleString`. The old `js_options_backup_file_count` key is replaced by `js_options_backup_last_local`.
- **Google Drive backup scope: `drive.file` → `drive.appdata`** (`manifest.json`, `gsBackup.js`): switched from the sensitive `drive.file` scope to `drive.appdata`, which stores backup files in a hidden app-specific folder not visible in the user's Drive UI. This eliminates the need for Google OAuth verification, making the Drive backup feature available to any Google account without restrictions. `getOrCreateDriveFolder()` removed, all Drive API calls now use `'appDataFolder'` as the parent directly. `getDriveFolderUrl()` returns `null` (no public folder URL exists for appdata). All file queries include `spaces=appDataFolder` as required by the Drive API.

### Removed
- **Automatic Tab Groups bug workaround dropped from the background** (`gsSession.js`, `background.js`): the historical recovery for grouped suspended tabs turning into `chrome://newtab/` after restart (crbug.com/522338670, issues #369/#374; `handleReplacedTabs` and the `onReplaced`/`onUpdated` hooks that fed it) has been removed. Chrome 150 fixes the bug natively and other Chromium browsers are aligning; the recovery logic remains available on demand in the Tab Health page (`health.js`).

### Fixed
- **Backup page: deleted Drive backups stayed selectable in the restore dropdown** (`backup.js`): deleting a file from "File di backup su Drive" (`buildFileRow`'s delete button) removed the row from that list but never touched the separate `driveBackupsSelect` restore dropdown, so a just-deleted backup remained pickable there until the page was manually refreshed, risking a restore attempt against a file that no longer exists. The delete handler now also removes the matching `<option>` from the restore dropdown (and its parent `<optgroup>` if left empty, and hides the restore card entirely if no backups remain).
- **Backup page: Drive backup dropdown showed a mangled time (`10 30:00` instead of `10:30:00`)** (`backup.js`): `formatOptionLabel` converted the filename's `HH-MM-SS` segment to a display time with a regex that only fixed up the last `MM-SS` pair, leaving a space where the `HH:MM` colon should be. Now splits the date and time segments on `T` and colon-joins the whole time part, correctly handling both current (`HH-MM-SS`) and legacy (`HH-MM`) filename formats.
- **Session backup: pinned state and real titles lost on restore** (`gsBackup.js`): `buildExportObject` only stored `url` and `groupId` per tab, so every restore from a local or Drive backup fell back to the tab's URL as its title and force-unpinned every tab. Now stores `title` (via `gsUtils.getCleanTabTitle`, same helper used for session history) and `pinned` per tab, and `importBackupJson` reads them back instead of hardcoding `title: tab.url` / `pinned: false`.
- **News feed: scheduled background refresh never populated the cache** (`gsNewsFeed.js`): `parseRssXml` used `DOMParser`, which doesn't exist in the extension's MV3 service worker (`background.js`); the alarm-driven and startup `fetchAndCache()` calls threw and were silently swallowed by the existing `catch`, so the feed (and unread badge) only ever updated when a user had `news.html` open. Replaced DOM-based parsing with regex-based RSS/HTML extraction that has no DOM dependency, so background refreshes now work.
- **Session backup ran in split-incognito context** (`gsBackup.js`): `performBackup()` had no incognito guard, so the separate service-worker instance that Chrome spins up for the extension in split-incognito mode could schedule and run its own backup alarm, exporting incognito-only tab URLs to the local downloads folder or Google Drive using the same (shared, `chrome.storage.sync`-based) backup settings. `performBackup()` now returns early when `chrome.extension.inIncognitoContext` is true.
- **Backup alarm not resynced after settings restore** (`backup.js`): importing settings (local file or Drive restore) wrote `AUTO_BACKUP_ENABLED`/interval/time directly to storage but never rescheduled the Chrome alarm, so an imported change to auto-backup only took effect after the next full browser/extension restart. `applySettingsJson()` now calls `gsBackup.syncAlarmWithSettings()` after saving.
- **Drive UI briefly showed stale destination after switching Local → Drive** (`backup.js`): `updateDriveAuthUI()` was called before `saveChange()` persisted the newly selected destination, so it read back the old value from storage and kept the Drive restore/settings panels hidden until a reload. Reordered so `updateDriveAuthUI()` runs after the new value is saved.
- **About page: doubled punctuation in the fork credit sentence** (`en/messages.json`): `html_about_fork_end` started with a leading comma (`", more information available on"`), which combined with the literal `"). "` already in `about.html` produced `"...(issue #1263). , more information..."`. Removed the leading comma and capitalized the sentence to `"More information available on"`, matching the Italian locale which was already correct.
- **Backup page: session backup settings laid out as a two-column label/control grid** (`backup.html`, `backup.css`): device name, interval, destination and max files to keep now sit in a `backupSettingsGrid` (`grid-template-columns: max-content 1fr`) instead of a wrapping flex row, so every field's label lines up in a left column and its control in a right column, consistent alignment regardless of label length, no more unpredictable wrapping.
- **Backup page: device name field capped at 30 characters** (`backup.html`, `backup.css`): reduced `maxlength` on the device name input from 40 to 30.
- **Backup page: "Connected account" labels now end with a colon like the settings grid** (`backup.html`): "Account"/"Cartella" (`html_backup_drive_label_account`, `html_backup_drive_label_folder`) were missing the trailing `:` that every other `.topLabel` in the page has, making them look visually different even after sharing the same CSS class.
- **Backup page: device name field shows the device ID as placeholder** (`backup.js`, `en/messages.json`, `it/messages.json`): when the device name field is empty, its placeholder now shows the actual device ID (e.g. `a91ca462`, same value shown on the debug page) instead of a generic "e.g. MacBook, Work PC" hint, since that ID is exactly what gets used as the fallback label wherever no custom name is set. Removed the now-unused `html_backup_device_name_placeholder` locale key.
- **Backup page: "Backup now" and Drive connect/disconnect actions on the same row** (`backup.html`, `backup.css`): `driveAuthActions` moved inside `backupConfigAction` instead of sitting in its own row below it, so the two related actions (run a backup now, manage the Drive connection) sit side by side.
- **Backup page: "Connected account" merged into the settings grid instead of its own box** (`backup.html`, `backup.js`, `backup.css`, `en/messages.json`, `it/messages.json`): the separate "Account collegato"/"Connected account" section (its own heading, `<dl>` grid, connect/disconnect buttons) is gone. Account and folder now appear as two more rows in `backupSettingsGrid`, shown only when the destination is Drive and a Drive account is connected, via nested `display: contents` wrapper elements (`.driveGridRows`) that let a parent/child pair of hidden toggles flatten into the same grid without breaking its row/column layout. The connect/disconnect buttons moved to a new `driveAuthActions` row below "Backup now", following the same settings-vs-action separation as the rest of the page. Removed the now-unused `html_options_backup_drive_account` locale key.
- **Backup page: "Connected account" (Drive) grid now matches the settings grid style** (`backup.html`, `backup.css`): `.driveInfoGrid` (`dt`/`dd` list for account and folder) switched from a custom mono/uppercase label style and a `1fr` stretching second column to reusing the shared `.topLabel` class and the same `max-content minmax(0, 280px)` layout as `.backupSettingsGrid`, so both grids on the page look and behave the same way.
- **Backup page: all settings controls now align to the same right edge** (`backup.css`): the grid's control column switched from `1fr` (stretching to fill the panel) to a fixed `minmax(0, 280px)`, and the device name input went back to `flex: 1` within that column instead of a fixed `22ch`. Interval, destination and max files selects already fill 100% of the column, so the device name input + "Save" button row now spans exactly the same width as the other fields instead of overshooting them.
- **Backup page: device name field now always visible, marked optional, and shown first** (`backup.html`, `backup.js`, `en/messages.json`, `it/messages.json`): the "Device name" field on the backup page was previously hidden unless other devices were already present in the Drive device registry, so it could appear unexpectedly after a fresh install once old multi-device backups were detected. It's now always shown, positioned above interval/destination/max files, with the label updated to "Device name (optional)" / "Nome dispositivo (opzionale)" to clarify it's not required.
- **Backup page: device name Save button height mismatch** (`backup.css`): the `btnSmall` class used on the device name row's "Save" button had no actual styling, so it fell back to the default 40px-tall `.btn`, which didn't match the shorter text input next to it. Both elements are now aligned to the same height.
- **Backup page: Google Drive restore row wrapping onto two lines** (`backup.css`): `.driveBackupPickerRow` used `flex-wrap: wrap` while the dropdown had a fixed `min-width: 150px`, causing the select and "Import selected" button to wrap in narrower cards. The dropdown now shrinks to fit instead of wrapping.
- **Health page: localization now respects TMS language setting** (`health.js`, `gsUtils.js`): all dynamic scan-result strings in the Tab Health page were using `chrome.i18n.getMessage()` (browser locale) instead of `gsUtils.getMessage()` (user-selected TMS locale). Replaced all calls; extended `gsUtils.getMessage()` to handle placeholder substitutions so the repair/restore button labels stay correct.
- **Shortcuts page: command descriptions ignored the TMS language setting** (`shortcuts.js`): `chrome.commands.getAll()` returns each command's `description` already resolved by Chrome using the *browser's* UI language, not the language selected in TMS settings. `chrome.runtime.getManifest()` turned out to apply the same browser-locale substitution to its `__MSG_xxx__` placeholders, so reading the description back from the manifest didn't help either. Descriptions are now re-resolved via a hardcoded command-name → message-key map (mirroring `manifest.json`'s `commands` section) passed through `gsUtils.getMessage()`, so they follow the same locale as the rest of the page. The browser-generated `_execute_action` description ("Activate the extension…") is not backed by a manifest message key and still follows the browser's language.
- **Unhandled "Could not establish connection" promise rejections in the background service worker** (`tgs.js`): three `chrome.tabs.sendMessage()` calls (`updateCommand`, `initSettings`, `showNoConnectivityMessage`) had no `.catch()`, unlike the equivalent call for `initTab`. Since the target tab can be closed or navigated away between the `contextGetByTabId` check and the actual send, the resulting rejection surfaced as an uncaught error in the console. All three now catch and log the error like the existing pattern.
- **Health page: Brave grouped-tab repair no longer silently fails** (`health.js`, `gsChrome.js`): the Brave recovery path now searches *all* saved sessions (not just the most recent one) for matching suspended URLs, fixing the case where `fetchLastSession()` returned `null` or the window-rank matching found no data. Added a visible warning when tabs cannot be recovered ("N tab(s) could not be recovered: no matching session data found."). Fixed `gsChrome.tabsGroup` to properly `return` after the `groupId === -1` guard and added `try/catch + reject` so failures on browsers that don't support `chrome.tabs.group` (Brave pre-v150) are caught and logged instead of silently hanging the repair flow; in that case the suspended tab is still created and the broken one removed, even if it can't be added back to the group.
- **Local backups could rotate one file too early** (`gsBackup.js`): backup filenames used minute resolution (`...T14-30.json`) and `chrome.downloads.download` runs with `conflictAction: 'overwrite'`, so two backups fired in the same minute (e.g. a scheduled alarm plus a manual "Backup now") produced the same filename, one file on disk but two distinct download IDs tracked in `tmsLocalBackupIds`. This overcounted backups and made rotation delete a still-wanted file one cycle early. Timestamps now include seconds; the filename regexes accept the seconds segment as optional so existing minute-only files still match for migration/cleanup.
- **Importing a malformed backup crashed with a raw TypeError** (`gsBackup.js`): `importBackupJson` only guarded `JSON.parse` and the top-level `windows` array, then iterated `win.tabs` unconditionally; a structurally valid but malformed file (a window with no `tabs` array) threw an uncaught `TypeError` instead of the clean `TMS_IMPORT_EMPTY`. Malformed windows are now skipped, and the import throws `TMS_IMPORT_EMPTY` if no usable window remains.
- **`unsuspendWhitelistedTabs` could throw when no window is focused** (`tgs.js`): `chrome.windows.getLastFocused` can hand back `undefined`, and `tab.windowId === currentWindow.id` then threw inside the async `chrome.tabs.query` callback as an unhandled rejection. The focused window id is now read with optional chaining (`currentWindow?.id`).
- **Drive → Local switch: offer to import latest backup as saved session** (`backup.js`, `backup.html`, `backup.css`, `en/messages.json`, `it/messages.json`): when the user switches the backup destination from Drive to Local, a modal dialog appears offering to import the most recent Drive backup directly into Sessions Manager. If accepted, the backup is downloaded from Drive and imported as a saved session in `history.html`, making it accessible without any Google Drive dependency. If Drive is not connected or has no backups, the modal is skipped silently. Clicking outside the dialog dismisses it.

- **Drive sections hidden when destination is Local** (`backup.js`): when the backup destination is set to "Local (Download folder)", the Drive-specific UI sections (restore from Drive, Drive files download panel, Drive settings card) are now hidden even if a Drive account is connected. The auth state is still tracked internally so switching back to Drive restores the full UI without re-authentication. The expensive Drive API calls (list backups, settings info) are also skipped, avoiding unnecessary network requests when Drive is not in use.

- **Local backup count unreliable due to stale chrome.downloads cache** (`gsBackup.js`, `backup.js`): `chrome.downloads.search` returns history entries whose `exists` field is stale (cached by Chrome and not reliably updated even when passing `exists: true` in the query), causing the "Backup archiviati" counter to show more files than actually exist on disk. Replaced the downloads-history-based count and rotation logic with explicit tracking in `chrome.storage.local` (`tmsLocalBackupIds`): on every backup the new download ID is appended; on every cleanup the oldest IDs beyond the limit are erased; `countLocalBackups` simply returns the list length. On first run after the update, the list is seeded from the existing downloads history (one-time migration). The count and rotation are now always accurate and independent of Chrome's internal cache.

- **Local backup collision and unbounded growth on multi-browser setups** (`gsBackup.js`, `en/messages.json`, `it/messages.json`): two related bugs fixed together.
  - **Filename collision**: local backups used a date-only filename (`tms-session-YYYY-MM-DDTHH-MM.json`). On a machine running both Chrome and Brave (or any two TMS installs sharing the same Downloads folder), both browsers could generate the same filename at the same minute; the second write silently overwrote the first. Fixed by switching `performLocalBackup` to `buildFilename()`, which embeds the per-install `deviceId` (e.g. `tms-session-a3f7b2c8-2026-07-01T14-30.json`), exactly like Drive backups already do.
  - **Cleanup never ran on Windows**: `cleanupOldLocalBackups` used `filenameRegex: 'tms-backups/tms-session-'` to search the downloads history, but on Windows `chrome.downloads` stores paths with backslashes, so the regex never matched and backup files accumulated indefinitely. Fixed by using `tms-backups[/\\]tms-session-` to accept both path separators, and by filtering results against `FILENAME_REGEX_NEW` (deviceId format) scoped to the current device, so each browser install manages only its own files. Legacy files (old date-only format) are preserved and excluded from rotation.

- **Chrome/Brave: grouped suspended tabs broken on browser restart** (`gsSession.js`, `background.js`): three distinct failure modes for grouped suspended tabs were identified and fixed. All three run within the existing 2-second startup window (or as a cache-based fallback for late events) and share the same create-in-group / remove-old pattern used by the original `onReplaced` Chrome/Edge path.
  - **Brave** (`recoverBraveGroupedTabs`): Brave fires no `onReplaced` and uses locale-specific "New Tab" titles, so the Chrome and Edge paths never activate. Grouped suspended tabs appear as `chrome://newtab/` after restart. Recovery matches each broken tab to the last saved TMS session (IndexedDB) by window order and tab index, then recreates it at the correct suspended URL in the same group. Addresses [#369](https://github.com/gioxx/MarvellousSuspender/issues/369).
  - **Chrome, discarded grouped tabs** (`recoverChromeDiscardedGroupedTabs`): on a full browser restart Chrome marks grouped suspended tabs as `discarded` (status `unloaded`) rather than loading them immediately. When the user first clicks one, Chrome's session-restore group bug redirects to `chrome://newtab/` instead of the suspended URL. Recovery pre-empts this by recreating all discarded grouped suspended tabs at startup, before any user interaction.
  - **Chrome, late `onUpdated` fallback** (`cacheGroupedSuspendedTab`, `handleGroupedTabToNewTab`): in case a grouped tab is not yet discarded when the startup sweep runs, a `tabId → suspendedUrl` cache is built from the `onUpdated {"groupId"}` events emitted at startup. If Chrome later fires `onUpdated` with `url: "chrome://newtab/"` for a cached tab, the tab is immediately redirected back to its suspended URL via `chrome.tabs.update`.

- **Drive backup files: download panel** (`backup.html`, `backup.js`, `gsBackup.js`, `backup.css`): collapsible `<details>` panel inside the "Restore from backup" section, shown only when Drive is connected and at least one backup file exists. Since Drive backups are stored in the hidden `appDataFolder`, users have no way to access them via the standard Drive UI; this panel fills the gap. The file list (name, date, size) is populated by `updateDriveAuthUI` reusing data already fetched, no extra API call on open. Each row has a "Download" button; files are grouped by device name when multiple devices are present, matching the existing restore dropdown behaviour. A download triggers `gsBackup.downloadDriveBackupAsFile(fileId, filename)`, which fetches the JSON content from Drive and saves it via `chrome.downloads.download` into the `tms-backups/` subfolder.
  - `gsBackup.downloadDriveBackupAsFile(fileId, filename)`, new public function.
  - `gsBackup.listDriveBackups()`, now also requests the `size` field from the Drive API.
  - New i18n keys (`html_backup_drive_files_title`, `html_backup_drive_download_btn`, `html_backup_drive_no_files`, `html_backup_drive_files_loading`) in `en` and `it` locales.
- **Debug: backup device identity card** (`debug.html`, `debug.js`, `debug.css`): new card in the Controls section showing the current device's backup ID (`gsBackupDeviceId`, 8-char hex) and friendly name. The ID is rendered in monospace with `user-select: all` so it can be copied in one click for support purposes. Populated at page load via `gsBackup.getDeviceId()` and `gsBackup.getDeviceName()`.
- **Debug: equal-height cards** (`debug.js`): all five option cards in the Controls grid are now forced to the same height after content is rendered. The tallest card (news feed, which includes a multi-field status line) previously made row 2 taller than row 1; `equalizeCardHeights()` measures the rendered heights and applies the maximum to every card so the two rows appear visually consistent.
- **Per-element "Saved" feedback in Options** (`options.js`, `style.css`): every option on the Options page now shows a brief "Salvato / Saved" confirmation inline after the changed control when the value is actually stored. The feedback fades in and disappears after 2 seconds; language changes are excluded (page reloads immediately). Implemented via dynamic span injection, no HTML changes required.
- **`unsuspendWhitelistedTabs` diagnostic logging** (`tgs.js`): added `gsUtils.log` instrumentation to the function, logs the current window ID, total tab count, whitelist content, per-tab original URL and match result, and deferred-tab count. Helps diagnose why the "Unsuspend whitelisted" action silently does nothing (likely empty whitelist or no URL match). The whitelist is now read once before the loop and passed directly to the synchronous `checkSpecificWhiteList`, which avoids one `chrome.storage` round-trip per tab.
- **Drive settings-backup status message layout** (`backup.css`): the "Saving to Drive…" / "Settings saved to Drive." status span now always appears on its own line below the action buttons instead of trailing inline after them (`flex-basis: 100%`).
- **Whitelist action links spacing** (`style.css`): "Verify whitelist" and "Unsuspend whitelisted" links now have consistent top margin separating them from the textarea above.
- **Inconsistent tooltip-icon spacing in Options** (`options.html`): removed trailing `&nbsp;&nbsp;` from six `data-i18n` label attributes; all help icons (?) now sit at a uniform distance from their label, governed solely by the flex `column-gap`.
- **XSS in restore status bar** (`backup.js`): `showRestoreStatus` now builds the DOM with `textContent` + a programmatically created `<a>` element instead of `span.innerHTML`, preventing injection via a maliciously crafted backup filename.
- **Imported tab titles** (`gsBackup.js` `importBackupJson`): restored tabs now use `tab.title || tab.url` instead of always setting `title: tab.url`, preserving the human-readable title from backup files that include it.
- **Null-deref in `removeTabFromSessionHistory`** (`gsIndexedDb.js`): added an early-return guard when `fetchSessionBySessionId` returns `null`, preventing a `TypeError` crash on stale or already-deleted session IDs.
- **aria-label hardcoded Italian** (`backup.html`): the Drive backup `<select>` now uses `data-i18n-aria-label` routed through the i18n system; added `html_backup_restore_drive_select_label` to `en` and `it` locales.
- **`replace(/\\s/, 'T')`** (`backup.js`): changed to `/\\s/g` so all whitespace characters in the Drive backup display name are replaced when reconstructing the source filename, not just the first.
- **`setForceScreenCaptureVisibility`** (`options.js`): replaced `style.display` manipulation with `classList.toggle('hidden', …)` per project CSP convention.
- **Backup config layout and number input styling** (`backup.html`, `backup.css`): the "Max backups per device" input is now on the same row as the interval and destination dropdowns. The "Esegui backup" button moved to its own row below, sized to `min-width: 150px` to match the select controls. The device name field (multi-device only) sits below with consistent spacing. `input[type='number']` and `input[type='time']` now share a combined CSS selector using design-system tokens (color, background, border, border-radius, padding, font, focus ring); the number input is capped at `width: 80px`.
- **News badge never visible** (`gsNavBadge.js`, all sidebar HTML files): the `<span id="navNewsBadge">` was a child of `<a data-i18n="...">`, `localiseHtml()` sets `innerHTML` on that anchor, destroying the span before `gsNavBadge.js` could find it. Fixed by moving `data-i18n` to an inner text `<span>` on all 7 pages (`about`, `backup`, `health`, `history`, `news`, `options`, `shortcuts`). The badge was therefore never shown to any user since the news feed launched.
- **News badge stale on open pages** (`gsNavBadge.js`): the badge was computed once at page load and never updated if storage changed while the page was open (e.g. after a background feed refresh). Replaced the one-shot read with a `chrome.storage.onChanged` listener so the badge appears or disappears in real time without requiring a page reload.
- **News badge pulse animation and alignment** (`style.css`): the badge dot now emits a red ping animation (`::before` pseudo-element, scale 1→2.6, 1.8 s loop) to draw attention. Size increased from 7 px to 8 px; added `top: -1px` to correct vertical alignment with the sidebar text.

### Localization
- **All 16 non-English/Italian locales completed** (`src/_locales/*/messages.json`): after PR #400 synced every locale's key set to match `en`, a large batch of strings introduced by recent features (Tab Health, Session Backup, News feed, ~164 keys) turned out to be an unedited copy of the English source in *every single locale*, not just a few. On top of that, `cs`, `sk` and `ja` were mostly untranslated overall (195/373, 127/373 and 167/373 keys respectively), and `fr-FR` was missing 141 keys outright plus had ~200 more left as English copies. Filled in translations for all of it directly (`fr-FR` reuses the now-complete `fr` translations, since it was a neglected duplicate rather than a genuinely distinct regional variant). A small number of remaining "identical to English" strings are legitimate cognates/loanwords (e.g. "min", "Local", "Sessions", "Backup" in several languages), not gaps. Will be reconciled back into Crowdin via its "sync from GitHub" import.

---

> **Rebase note (2026-06-25):** both `feature/session-backup` and `feature/visual-redesign` rebased onto `master` v8.1.6. Incorporated: CSP favicon fix for history page (`gsFavicon.js`, `historyItems.js`), `getDebugInfo` moved from `tgs.js` to `debug.js` with `promiseWithTimeout`, prevent extension tabs from being suspended (`gsUtils.js`).

---

## [9.0.0] — 2026-06-24

### Added
- **Persistent log buffer and diagnostic report** (`debug.html`, `debug.js`, `gsUtils.js`): errors are now always captured to an in-memory ring buffer (max 500 entries) and flushed to `chrome.storage.local`, surviving Service Worker restarts. Warnings and verbose `log()` calls are captured when the new **captureLogs** flag is enabled. The flag persists to storage so it survives SW restarts; toggling it from the Diagnostic page immediately wakes the SW via `chrome.runtime.sendMessage`. `debug.html` is now a proper **Diagnostic page** with a log viewer (colour-coded by level), "Refresh", "Clear log", "Copy report" and "Download report" buttons. The report bundles extension version, browser user-agent, current tab profiler snapshot and the full log buffer into a single shareable text file, making it actionable for end-users who cannot access DevTools.
- `gsStorage.CAPTURE_LOGS` (`gsCaptureVerbose`) and `gsStorage.LOG_BUFFER` (`gsLogBuffer`) storage key constants.

### Changed
- **`updated.html` / `updated.js`**: after the automatic tab-restore completes, `suspendy-guy.webp` (in-progress) is now swapped to `suspendy-guy-success.webp` (success) via `toggleUpdated()`.
- **`gsUtils.js`**: added `getMessage(key)` helper that reads from `_localeMessages` first (custom language override) and falls back to `chrome.i18n.getMessage()`, ensuring the user-chosen language is respected everywhere.
- **`popup.js`**: replaced all `chrome.i18n.getMessage()` calls with `gsUtils.getMessage()` so that the status detail text and "Backup now" label honour the language selected in options rather than the browser UI locale.
- **`debug.html` → Diagnostic page**: removed the defunct `debugInfo`/`debugError` DevTools-only toggles (they only affected the page's own JS instance, not the Service Worker). Replaced with the `captureLogs` persistent toggle. Instructions no longer mention "background page" (MV2 terminology). Sections restructured: Controls → Log buffer → Tab profiler.
- **`gsUtils.error()`**: always buffers to storage regardless of `debugError` flag; error path no longer has a dead commented-out else branch.
- **`gsUtils.warning()`**: stack-trace append inlined into `console.warn` call (no more mutation of `args` array).
- **`background.js`**: reads `gsCaptureVerbose` from storage at Service Worker startup to restore the flag; adds `setCaptureLogs` message handler. Removed large commented-out developer-only debug block.
- **Locale** (`en`, `it`): updated `html_about_debugging_prefix` / `html_about_debugging_suffix` to describe the diagnostic report workflow instead of the old "set debugging flags" copy.

### Removed
- Dead `debugWindowId` `innerHTML` assignments (4 commented-out lines) from `historyUtils.js`.

### Fixed
- **`gsTabCheckManager.checkSuspendedTab()`, grouped suspended tabs lost after Chrome restart**: Chrome's tab-group replacement bug (`crbug.com/522338670`) can navigate a suspended tab's reload to `chrome://newtab/` instead of restoring `suspended.html`. When the initialisation check detects no active extension context after a resuspend attempt, it now fetches the tab's current state: if the reload navigated away from the suspended URL and the tab was in a group, a fresh tab is created with the original suspended URL, added to the same group, and the broken tab is removed. Pressing Back in the browser to recover the suspended page is no longer necessary.
- **`gsUtils.isSpecialTab()`, extension pages treated as normal tabs**: `chrome-extension://` URLs were not matched by `isBrowserInternalURL` (whose regex tests for `chrome:`, not `chrome-extension:`), so TMS's own pages (`debug.html`, `about.html`, `options.html`, …) were classified as normal tabs. This caused the check queue to repeatedly attempt content-script injection (failing with "Cannot access contents of url"), assign a suspension timer, and leave them as `unknown` in the profiler. A dedicated `startsWith('chrome-extension://')` guard is now applied before the `isBrowserInternalURL` call.
- **`tgs.getDebugInfo()`, `unknown` status for whitelisted/pinned/audible tabs**: when the content script did not respond (tab loading, whitelisted, pinned, or other), the profiler was short-circuiting and leaving the status as `unknown` instead of falling through to `calculateTabStatus`. Now `calculateTabStatus` is always called, passing the content-script status only when available, so the profiler correctly shows `whitelisted`, `pinned`, `audible`, `active`, `never`, etc.
- **`tgs.getDebugInfo()`**: restored the suppressed `gsUtils.warning()` call on content-script request failure.

## [9.0.0] — 2026-06-24 (dependencies)

### Added
- **"Wake whitelisted tabs" action**: new command that finds all suspended tabs whose original URL matches the whitelist and unsuspends them. Available in the extension popup (below "Unsuspend all tabs") and in the Options page next to the "Test whitelist" link (separated by a `·`). Implemented in `tgs.unsuspendWhitelistedTabs()`, routed via `background.js` on action `unsuspendWhitelisted`.
- `en` and `it` locale files: added `html_options_whitelist_unsuspend` and `html_popup_unsuspend_whitelisted_tabs`.
- **Automatic session backup** (`src/js/gsBackup.js`): new module that silently saves the current session as a JSON file to the user's default Downloads folder (inside a `tms-backups/` subfolder) at a configurable interval. Uses `chrome.downloads` API with `saveAs: false` and `conflictAction: 'overwrite'` so no dialog appears. Compatible with MV3 service workers (uses a `data:` URL instead of a Blob URL).
- `manifest.json`: added `"downloads"` permission, required to write files to disk from the background service worker.
- `gsStorage.AUTO_BACKUP_ENABLED` / `gsStorage.AUTO_BACKUP_INTERVAL`: two new settings keys (defaults: `false` / `'1'` hour).
- **Backup settings UI** in `options.html`: new "Automatic session backup" section (id `#section-backup`) with: enable checkbox, plain-text description below it (`.sectionDesc`, no box), interval selector (15min, 30min, 1h, 2h, 4h, 8h, 24h) and "Test backup now" button on the same row (`.backupIntervalRow`). The test button detects whether Chrome will download silently: it checks the download state after 1.5 s and shows either a reassurance message or a warning to disable "Ask where to save" in Chrome settings. Backup filenames include date and time (`tms-session-YYYY-MM-DDTHH-MM.json`). Up to 10 backups are retained; older ones are deleted automatically via `chrome.downloads.removeFile()` + `erase()`.
- **In-page section nav** in `options.html` and `history.html`: sticky tab-underline nav that stays pinned at the top of the content area while scrolling. The active tab updates automatically via a scroll listener (highlights the section currently in view). Nav styles live in `style.css` (shared). History sections received stable `id` attributes (`section-current/recent/saved/migrate`).
- **Back-to-top button** in `options.html`: fixed button in the bottom-right corner, fades in after 200 px of scroll and scrolls back to top smoothly on click.
- **Backup settings link** in `history.html`: small note at the top of the Sessions page pointing to `options.html#section-backup` for discoverability.
- `en` and `it` locale files: added `html_options_backup_title/enable/enable_tooltip_line1+2/interval/note/now`, `html_options_back_to_top`, `html_history_backup_settings_link`, `js_options_backup_now_running/done/error`, `js_options_backup_next_run`, `js_options_backup_file_count`.
- **Backup metadata** in `options.html`: below the interval row, a small muted line shows the next scheduled run time (from `chrome.alarms`) and the number of backup files currently archived on disk (from `chrome.downloads`). Both update live when the interval or enable state changes, and after a manual test backup.
- **Google Drive backup destination** (`src/js/gsBackup.js`): users can now choose between saving backups locally (Downloads folder) or to Google Drive. When Drive is selected, the extension authenticates silently via `chrome.identity.getAuthToken()` (OAuth 2.0, `drive.file` scope) and uploads the session JSON to a visible "TMS Backups" folder in the user's Drive via the Drive REST API. Cleanup of old Drive backups (keeping the 10 most recent) is handled automatically. The local `chrome.downloads` path is unchanged for users who prefer it.
- `manifest.json`: added `"identity"` permission and `"oauth2"` block (client_id placeholder, `drive.file` scope); updated CSP `connect-src` to allow `https://www.googleapis.com` and `https://accounts.google.com`.
- `gsStorage.AUTO_BACKUP_DESTINATION`: new setting key (default: `'local'`; values: `'local'` | `'drive'`).
- **Drive destination UI** in `options.html`/`options.js`: radio buttons to choose Local vs Google Drive; "Connect Google Account" / "Disconnect" buttons; live connected-account email display. The Drive auth panel shows/hides automatically based on the selected destination.
- `en` and `it` locale files: added `html_options_backup_destination`, `html_options_backup_dest_local/drive`, `html_options_backup_drive_connect/disconnect`, `html_options_backup_drive_account`, `js_options_backup_drive_connecting/auth_error`; updated `html_options_backup_enable`, `html_options_backup_enable_tooltip_line1+2` and `html_options_backup_note` to be destination-agnostic.
- **Settings export / import** (`options.html` / `options.js`): two buttons in the General section allow exporting all extension settings to a dated JSON file (`tms-settings-YYYY-MM-DD.json`) and importing them back. Import validates the file, asks for confirmation, merges only known keys against defaults, saves and syncs, then reloads the page. Prevents data loss during reinstalls or developer reloads.
- `en` and `it` locale files: added `html_options_settings_backup/export/import`, `js_options_settings_import_confirm/success/error`.
- **Manual backup button in popup**: when automatic session backup is enabled (local or Drive), a new entry appears in the popup above the Settings/Session manager links. The label includes the active destination: "Backup session now (Local)" or "Backup session now (Cloud)". Clicking it fires an immediate `performBackup()` via the `backupNow` message action and closes the popup. Hidden when backup is disabled. New locale keys: `html_popup_backup_now_local`, `html_popup_backup_now_cloud`.

### Changed
- **`gsIndexedDb.js`**: replaced abandoned `db.js` (Aaron Powell, last commit 2021) with [`idb` v8.0.3](https://github.com/jakearchibald/idb) (Jake Archibald, ISC). All IndexedDB operations rewritten to use `openDB`, `getAllFromIndex`, `getAll`, `getAllKeys`, `add`, `put`, `delete`, `clear` directly on the idb-wrapped database handle. The `server` cache field renamed to `_db`; `DB_VERSION` changed from string `'3'` to numeric `3` (required by idb). Logic and behaviour are identical; the upgrade path preserves existing stores.
- **`html2canvas`**: updated from `1.0.0-rc.7` (2020 release candidate) to `1.4.1` (2022 stable release).
- **`about.html`**: credits updated to reflect idb replacing db.js.
- **`package.json`**: `dependencies` updated from `db.js ^0.15.0` to `idb ^8.0.3`.
- **`options.html`**: removed "Settings backup" row from the General section and the entire "Automatic session backup" section; removed the corresponding entry from the inline section nav. Sidebar now includes a link to `backup.html` positioned after "Sessions".
- **`backup.html`** UX refinements: removed the in-page jump nav (not enough content to warrant it); added a descriptive text for the Settings backup section; renamed "Test backup now" → "Backup now" / "Esegui backup"; interval selector and destination radios now appear on the same row; a contextual info panel below them shows Drive account info (if Drive is selected) or the local folder path (if Local); a transient "Saved" confirmation appears inline when any session backup option changes; "Backup" sidebar entry repositioned immediately after "Sessions" across all pages.
- **`options.js`**: removed all backup-related code (imports, `elementPrefMap` entries, helper functions, event listeners).
- **Fontello → Lucide SVG sprite**: replaced the custom fontello icon font (6 glyphs, woff/woff2) with a minimal Lucide SVG sprite (`src/img/icons.svg`) now containing `circle-help`, `chevron-right`, `chevron-up`, `chevron-down`, `square`, `square-check`, `square-plus`, `square-minus`, `shield-check`, `rotate-cw`. Tooltip icons in `options.html` and `backup.html` are now `<svg><use href="img/icons.svg#…">` elements. Custom checkboxes, the `<select>` dropdown arrow, and whitelist action link icons in `style.css` use `mask-image` with inline SVG data URIs (supports CSS custom-property colours). The session expand/collapse icon in `historyItems.js` / `history.js` is created via `createElementNS` and toggled by `data-icon` attribute + `href` swap. `fontello.css`, `fontello.woff`, `fontello.woff2` removed; credits in `about.html` updated to Lucide (ISC). Select dropdown arrow managed via JS (`gsUtils.initSelectArrows`) with `focus`/`blur`/`change`/`mousedown` listeners; arrow shows `chevron-down` when closed, `chevron-up` when open.
- **CSS consolidation**: `about.css`, `health.css`, `history.css`, `options.css` (7–30 lines each, single-page) merged into `style.css` under clearly labelled section comments. Remaining separate files: `backup.css`, `suspended.css`, `popup.css`, `debug.css`. Whitelist action icons (`#testWhitelistBtn`, `#unsuspendWhitelistedBtn`) upgraded from Unicode `▶`/`↺` to Lucide `shield-check`/`rotate-cw` via CSS `mask-image`.
- **Brand header tightening**: `.brandTitleText` gap reduced to `0` and `.brandMark h1` line-height reduced to `1` to bring the extension title closer to the version/links row below it.
- **Tooltip icon alignment**: `.formRow` now uses `display: flex; align-items: center; flex-wrap: wrap` so the checkbox, label and tooltip icon are vertically centred without relying on `vertical-align` hacks. `.formRow > .gsNote` gets `flex-basis: 100%` to wrap to its own line. Fixed `setAutoSuspendOptionsVisibility` and `setForceScreenCaptureVisibility` in `options.js` to restore `display: flex` (was `block`) so dynamically shown `.formRow` elements keep the flex layout.
- **Button style consistency**: `health.html` and `shortcuts.html` action buttons changed from `btn` (filled primary) to `btn btnNeg` (outline), matching every other page in the extension.
- **`history.html`**: the "Looking for automatic session backups?" hint now links to `backup.html` instead of `options.html#section-backup`.
- **Backup scheduling, wall-clock alignment**: backup alarms are now anchored to real clock time instead of resetting on every browser restart. Sub-daily intervals (15 min, 30 min, 1 h, …) snap to the nearest upcoming grid slot aligned to midnight (e.g. 15 min → always fires at :00/:15/:30/:45). The "once a day" option now requires the user to select a specific time of day via a new `<input type="time">` field; that picker appears only when the 24 h interval is selected, and defaults to 09:00. A new storage key `gsAutoBackupTime` persists the chosen time. Implemented in `gsBackup.scheduleBackup()` using `chrome.alarms` `when` parameter instead of `delayInMinutes`. New locale keys: `html_options_backup_daily_time` (en + it).
- **Settings export filename**: removed the time component, the file is now named `tms-settings-YYYY-MM-DD.json`. Unlike session backups, settings are not versioned by the minute; exporting twice in the same day simply overwrites the previous file.
- **Settings backup to Drive**: when Google Drive is selected as backup destination and an account is connected, a "Save settings to Drive" button appears in the Settings backup section. It uploads `tms-settings.json` to the TMS Backups folder, overwriting any existing file (singleton, not subject to the 10-backup session rotation). Implemented in `gsBackup.performDriveSettingsBackup()` using Drive search + PATCH for updates, POST for first creation. New locale keys: `html_backup_drive_save_settings`, `js_backup_drive_settings_saving`, `js_backup_drive_settings_saved`, `js_backup_drive_settings_error` (en + it).
- **Drive auth error badge + popup banner**: if a backup fires (automatically or from the popup) while Drive is selected but no Google account is connected, the extension icon shows a red `!` badge and a flag is stored in `chrome.storage.local`. The next time the popup is opened, a red banner appears with a message and a click action that opens `backup.html`. Badge and flag are cleared automatically when the popup is opened or when a Drive backup succeeds. No extra permissions required. New locale key: `html_popup_backup_drive_error` (en + it).
- **Drive account section redesign** (`backup.html`): the "Account collegato" heading is now a `pageTitle` shared between the connected and disconnected states (moved outside both inner divs). The connected-state info is restructured as a CSS grid definition list (`.driveInfoGrid`) with two rows, Account (name + email) and Cartella (folder name + open link), aligned in two columns with muted monospace labels. The Google Drive favicon is loaded via `chrome-extension://…/_favicon/` (uses the existing `favicon` permission) and shown next to "TMS Backups". New locale keys: `html_backup_drive_label_account`, `html_backup_drive_label_folder` (en + it).
- **Drive folder link**: when a Google account is connected, a direct link to the 'TMS Backups' folder on Drive appears inside the definition list. The URL is resolved via `gsBackup.getDriveFolderUrl()`. Hidden when disconnected.
- **Backup status row**: "Backup salvato." now appears inline next to "Prossima esecuzione" with matching font size and color, separated by a `·` added via CSS `::before`. Both share a `.backupStatusRow` flex container. Status and next-run spans moved outside `backupConfigAction` to avoid interference with the button row layout.
- **Status messages layout**: `settingsImportStatus` moved outside `.settingsBackupBtns` flex container so all feedback messages (saving, saved, error) wrap consistently on their own line regardless of text length.

### Removed
- `src/js/db.js`: vendored copy of the abandoned db.js library removed.
- **Dedicated Backup page** (`src/backup.html`, `src/js/backup.js`, `src/css/backup.css`): all backup-related UI has been extracted from `options.html` into a new first-class page. The page contains two sections: "Settings backup" (export/import of extension settings, previously at the bottom of the General section) and "Automatic session backup" (schedule, destination, Drive auth, previously `#section-backup`). A "Backup" entry is now visible in the sidebar navigation of all pages.
- `en` and `it` locale files: added `html_sidebar_backup`, `html_backup_page_title`, `html_backup_settings_desc`, `html_backup_local_folder_hint`, `js_backup_option_saved`. Updated `html_history_backup_settings_link` to point users to the new Backup page instead of Settings.

- **Sessions vs Backup, conceptual clarity**: `history.html` now opens with a `sectionDesc` paragraph explaining that sessions live inside Chrome and disappear if the browser profile is lost. The existing muted hint linking to the Backup page has been replaced by a `.gsNote--warn` warning box to make the risk visible at a glance. `backup.html` gains a `sectionDesc` under "Automatic session backup" explaining that backups are saved outside Chrome and survive crashes. The `.gsNote--warn` modifier has been promoted from `backup.css` to `style.css` so it is available on all pages. New locale keys: `html_history_page_desc`, `html_backup_session_desc` (en + it). Updated keys: `html_history_backup_hint_prefix`, `html_history_backup_settings_link` (en + it).

### Fixed
- **Recovery mascot images**: in-progress state uses `suspendy-guy-oops.webp`; success state uses `suspendy-guy-success.webp`. Converted `suspendy-guy-success.png`, `suspendy-guy-alt.png`, `suspendy-guy-marvellous-alt.png`, `suspendy-guy-marvellous.png` to WebP (~77% size reduction each) and removed the original PNGs.
- **Italian locale, recovery success string**: "…visita la [gestione delle sessioni] pagina" riformulato in "…vai alla [gestione delle sessioni]", eliminando la parola "pagina" sospesa che spezzava la frase.
- **Recovery page stuck on spinners**: after clicking "Restore tabs automatically", the spinner rows never disappeared because `removeTabFromList` was only called at page load (ping of already-suspended tabs) or on manual tab click, never after `recoverLostTabs()` completed. Now `showRecoveryComplete()` is called directly when `recoverLostTabs()` resolves, so the success screen always appears. Also refactored the inline `style.display` assignments in that path to use the existing `.reallyHidden` CSS class.

### Meta
- Branch rebased onto `feature/visual-redesign` (2026-06-24), which was itself rebased onto `master` to incorporate v8.1.5 (#374 Edge tab group bug fix in `background.js`/`gsSession.js`, #376 tab recovery for non-suspended tabs). No conflicts on our session-backup-specific files.

## [9.0.0] — 2026-06-24 (visual redesign)

### Added
- `CHANGELOG.md` to track the visual redesign initiative.
- `src/css/tokens.css`: design system tokens aligned with marvellouscode.works ("Technical Blueprint" palette). Includes:
  - OKLCH color tokens for light and dark modes.
  - Dark mode applied via OS preference (`@media (prefers-color-scheme: dark)`) and via JS-controlled `body.dark` / `body.light` classes.
  - Self-hosted variable fonts: Inter (sans-serif) and JetBrains Mono (monospace), loaded via `@font-face` with `font-display: swap`. Fallback stacks preserved.
  - Spacing (4px grid), border-radius, type scale, shadows, motion, and layout tokens.
  - `@media (prefers-reduced-motion: reduce)` sets all duration tokens to `0ms`.
- `src/font/inter-variable.woff2` + `src/font/OFL-Inter.txt`: Inter variable font (SIL OFL).
- `src/font/jetbrains-mono-variable.woff2` + `src/font/OFL-JetBrainsMono.txt`: JetBrains Mono variable font (SIL OFL).
- `src/css/history.css`: page-scoped stylesheet for `history.html`. Defines `.migrate-input-row` flex layout for the tab-migration input row.
- `src/css/options.css`: page-scoped stylesheet for `options.html`. Overrides `.welcome-message` to stretch full-width (`max-width: none`).
- `src/_locales/en/messages.json` and `src/_locales/it/messages.json`: added 4 new i18n keys (`html_history_migrate_section_title`, `html_history_migrate_section_desc`, `html_notice_title`, `html_notice_important_notice`) used by the redesigned history page.
- **Language override**: new dropdown in the General section of Settings lets the user force a specific UI language regardless of the browser's system locale. Implemented via `gsStorage.LANGUAGE` preference (default `'auto'`), `gsUtils.loadLocaleMessages()` (fetches the chosen `_locales/{locale}/messages.json` at page load), and a modified `localiseHtml()` that prefers the cached map over `chrome.i18n.getMessage()`. Changing the language triggers a page reload. Note: the override applies to all extension UI pages (options, popup, suspended tab); manifest-level strings and background-generated tab titles remain under Chrome's locale.
- `en` and `it` locale files: added `html_options_language` and `html_options_language_auto` i18n keys. Other locales will be updated via Crowdin.

- **Tab strip context menu** _(pending Chrome stable rollout)_: TMS now registers its actions in the right-click menu on the tab bar itself via Chrome's new `"tab"` context type, alongside the existing page-content context menu. Appears when the existing "Add context menu" option is enabled. The menu exposes: Suspend/Unsuspend this tab, Pause/Unpause auto-suspension, Never suspend domain, Never suspend URL, Suspend/Unsuspend other tabs in window, and Suspend/Unsuspend all tabs in all windows. The per-tab actions operate on the right-clicked tab specifically, not necessarily the active tab. The Chrome API accepts the registration without errors but the tab strip UI rendering has not yet shipped in Chrome stable (confirmed on Chrome 149); the implementation is ready and will activate automatically once Chrome ships the feature. Implemented via three new `tgs.js` functions (`toggleSuspendStateOfTab`, `requestToggleTempWhitelistStateOfTab`, `whitelistTab`) and new `tab_*` cases in `background.js`'s `contextMenuListener`.

### Changed
- All PNG images in `src/img/` converted to WebP (quality 90) for smaller bundle size. HTML pages, `src/js/gsFavicon.js`, and `src/js/health.js` updated to use WebP. Notable savings: `ic_suspendy_128x128` 23 KB → 5.8 KB, `marvellous-codeworks-logo` 38 KB → 13.7 KB. Original PNG files archived to `MC-Assets-Private`. Extension toolbar icons (`manifest.json`, `src/js/tgs.js`) intentionally kept as PNG: Chrome's extension system only officially supports PNG for `icons` / `action.default_icon` / `chrome.action.setIcon()`.
- All HTML pages: brand subtitle updated from "a project by Marvellous Codeworks" to "a Marvellous Codeworks project".
- `en` and `it` locale files: theme option `html_options_suspend_theme_system` renamed from "Automatic" to "System" / "Sistema". Other locales will be updated via Crowdin.
- `src/options.html`: further reorganised settings sections. "Other" section renamed to "General" (all locales updated) and moved to first position, it now contains the language selector, theme, context menu, and sync settings. "Suspend on low memory" (`suspendInPlaceOfDiscard`) moved from "Other" to "Suspend" (with other suspension triggers). Theme selector moved from "Suspended tabs" to "General" (extension-wide preference).
- `src/css/style.css`: full rewrite consuming `tokens.css` variables. Primary accent color changed from `#3477db` (blue) to `oklch(0.45 0.18 158)` (teal-green, matching marvellouscode.works). Settings layout widened to `--content-max` (1120px). Buttons redesigned: monospace font, uppercase tracking, near-zero radius. Dark mode now uses proper token cascade instead of hardcoded hex overrides. Tab group colors preserved unchanged; dark-mode variants migrated from nested `body.dark { … }` (Chrome-only syntax) to flat `body.dark span.group.…` selectors for broader compatibility. Added `.brandMark` / `.brandLogo` for settings page header, `.pageTitle` eyebrow, and redesigned `.splash` / `.splash-wrap` as proper card with grid background.
- `src/css/popup.css`: rewritten to consume `tokens.css` variables. Hardcoded hex colors (`#3477db`, `#777`, `#fff`, `#222`, `#ccc`) replaced with token references. Dark/light mode handled by token cascade, the previous `@media (prefers-color-scheme: dark)` overrides and explicit `.dark` / `.light` blocks are removed.
- `src/css/suspended.css`: now imports `tokens.css`. Hardcoded values replaced with token references (`--muted-foreground`, `--primary`, `--shadow-md`, `--duration-base`, `--card`, `--border`). `rgba()` shadows replaced with `oklch()` equivalents.
- All 5 settings pages (`options.html`, `about.html`, `health.html`, `shortcuts.html`, `history.html`): unified `pageHeader` with `marvellous.png` brand logo + extension name (monospace h1). Sidebar `<div class="contentNav">` changed to `<nav>`. Each content area gets a `.pageTitle` eyebrow. All JS-hooked IDs and class names preserved.
- All 5 settings pages: `pageHeader` brand mark redesigned, extension icon (`ic_suspendy_128x128.png`, 40 px) leads the left side next to the extension name (`--text-2xl`, bold); official Marvellous Codeworks logo (`marvellous-codeworks-logo.png`, sourced from the marvellouscode.works repo) with "a project by Marvellous Codeworks" label is pinned to the right via `justify-content: space-between`. New CSS classes: `.brandTitle`, `.brandCodeworks`, `.brandSubtitle`; `.brandLogo` at 20 px. `marvellous.png` placeholder no longer used in the header.
- `src/about.html`: revised page content, added extension tagline (from marvellouscode.works), version label with context, compact credits section with `pageTitle` heading, inline links to GitHub repo / official site / KB (with GitHub SVG icon and MC logo), contributor acknowledgment including link to the contributors graph. Removed BrowserStack credit (no longer a dependency). Added new i18n keys: `html_about_version_label`, `html_about_tagline`, `html_about_section_credits`, `html_about_contributors_and`, `html_about_community_thanks_link`. Also removed the three stale `html_about_browserstack_*` keys from `en/messages.json`.
- All 6 standalone/splash pages (`recovery.html`, `broken.html`, `update.html`, `updated.html`, `permissions.html`, `restoring-window.html`): markup cleaned up, `alt=""` added to decorative images. `recovery.html`: secondary button class changed from `secondary` to `btnNeg` (matches button system). `permissions.html`: heading changed from `h2` to `h1` for correct heading hierarchy.
- `src/_locales/it/messages.json`: complete Italian localisation overhaul, translated all ~45 strings that were still in English (entire `html_health_*` section, tooltip blocks for sync/discard/screen-capture/low-memory, update/broken page strings, sidebar labels, migrate success message, shortcut command label); standardised terminology (`tab` → `scheda`, `whitelist` → `lista bianca`, `form` → `campi modulo`); fixed grammatical issues (`po 'di` → `po' di`, `altro.` → `altre.`, `html_broken_title` gender agreement); `html_welcome_title` changed from "Ciao" to "Benvenuto".
- `src/css/tokens.css`: primary color shifted from green-teal (hue 158°) to cyan-blue (hue 210°) to match the mascot's eye-glow signature color. Affects `--primary`, `--primary-hover`, and `--ring` in all three theme blocks (`:root`, `@media (prefers-color-scheme: dark)`, `body.dark`, `body.light`). Dark-mode chroma reduced from 0.18 to 0.14 to avoid over-saturation on dark backgrounds.
- `src/css/popup.css`: `#header` default state changed from near-white `--muted` background to `--muted` background with `--primary` text, giving the status bar a branded cyan accent without competing with the `willSuspend` (full `--primary` bg) and `blockedFile` (full `--destructive` bg) states. `#statusDetail a` color updated to `--primary-hover` for legibility on the light background.
- All 11 HTML pages: `extensionIcon` image in `.brandTitle` changed from `ic_suspendy_128x128.png` to `suspendy-guy-main.png`.
- `src/css/popup.css`: `#statusDetail a` color changed from `--primary-hover` to `--foreground` with `text-decoration: underline` so links are clearly distinguishable from the surrounding `--primary` status text on the `--muted` background.
- `src/css/popup.css`, `src/popup.html`, `src/js/popup.js`: added `#brand` bar at the top of the popup, robot icon (`suspendy-guy-main.png`, 24 px), extension name (monospace bold), and version number (`vX.Y.Z` read from `chrome.runtime.getManifest().version`, populated in `showPopupContents`). Styled with `--background` fill and a `--border` bottom separator.
- `src/css/debug.css`: removed `max-width` / `margin: auto` centering from `.debugWrap`. Page now starts flush to the left edge with `--space-4` padding on each side and a reduced top margin, giving the profiler table the full horizontal space.

### Removed
- `en/messages.json` and `it/messages.json`: deleted 25 orphaned i18n keys no longer referenced by any HTML or JS file: `html_about_github_title`, `html_about_blog_title`, `html_about_twitter_title`, `html_about_html2canvas{,_prefix,_suffix}`, `html_about_indexedDb{,_prefix,_suffix}`, `html_about_tgs_{prefix,suffix}`, `html_notice_{title,important_notice}`, `html_popup_dont_suspend_now`, `html_suspended_tab_{suspended,reload,whitelist_prefix,whitelist_suffix}`, `html_updated_{detail_header_prefix,detail_header_suffix,new_features_header,bug_fixes_header,release_notes}`, `html_welcome_title`, `js_suspended_remove_from_whitelist`.

### Fixed
- **CSP violation on suspended tab page**: `suspended.html` uses `style-src 'self'` (no `unsafe-inline`), which caused Chrome to block all `element.style.*` property assignments. Replaced every inline-style mutation in `suspended.js` with CSS-class toggles: `img-preview-mode` / `preview-scrollable` control preview visibility and body overflow; `toast-active` triggers the connectivity-toast animation; `update-available` shows the update banner. Corresponding CSS rules added to `suspended.css`.
- All HTML pages: added `<meta charset="UTF-8">` to `<head>`. The missing declaration caused multi-byte characters (Arabic, Czech, French, Japanese, Portuguese, Russian, Sinhala, Slovak, Turkish, Chinese) in the language selector dropdown to render as mojibake.
- `src/css/popup.css`: aligned popup typography with the rest of the extension. `.menuOption` font explicitly set to `--font-sans` (Inter, same as option labels in `options.html`) and size corrected from `--text-md` (16px) to `--text-base` (14px). `line-height: 2em` replaced with explicit `padding: --space-2 --space-4` + `line-height: 1.5`. Hover state changed from `text-decoration: underline` to `background-color: --accent` (same as sidebar nav). `#header` gains `--font-mono` and a `border-bottom` separator. Padding tightened from `--space-4` to `--space-3 --space-4`.
- All 6 splash pages: added `.splash-header` brand mark (extension icon + name + Codeworks logo/label) as a full-width row spanning the two-column splash grid, bringing visual identity in line with the settings pages.
- `src/debug.html`: restructured with `.debugWrap` container, `.debugHeader` brand mark, `.pageTitle` eyebrow, and semantic wrappers for the controls, instructions, and profiler table, replacing the old bare `<body>` layout.
- `src/css/debug.css`: rewritten to consume `tokens.css` variables (`--content-max`, `--space-*`, `--font-mono`, `--text-*`, `--border`, `--muted-foreground`). Removed hardcoded `font-size: 1.3rem` and `padding: 20px`.
- `src/css/style.css`: added `.splash-header { grid-column: 1 / -1; … }` utility class for the splash brand row.
- `src/js/debug.js`: removed `setAttribute('href', 'chrome://extensions/…')` that caused a "Not allowed to load local resource" console error. Navigation now handled exclusively by the `onclick` handler via `chrome.tabs.create`.
- `src/css/style.css`: added `min-width: 0` to `.splash > div:not(.splash-header)` so the `1fr` content column can actually shrink and constrains its children correctly. Added `overflow: hidden` to `.tabContainer` to create the clipping context required for `a.historyLink`'s `text-overflow: ellipsis` to work; without this, long tab titles/URLs in `recovery.html` (and `history.html`) would overflow the splash card.
- `src/health.html`: removed `sub-section` class from `#scanProgress` (progress bar was picking up `border-bottom + padding + margin` meant for layout sections, creating empty separator rows below the scan bar). Removed `sub-section` class from `#actionIntro` (same reason, caused a dangling border inside `#actionSection`). Removed redundant `<hr />` from `#copySection` (the element already has `.sub-section` border-bottom).
- `src/js/gsTabCheckManager.js`: wrapped `chrome.tabs.sendMessage` call in `try/catch` when fetching `suspendInfo` from a suspended tab. Previously an unhandled messaging error (e.g. tab not ready) would propagate and break the check loop; now the error is logged and the tab is requeued with `refetchTab: true`.

### Meta
- `.gitignore`: added `/.claude` and `/.tokensave` to exclude local AI-tooling directories from version control.
- `src/js/background.js`: removed erroneous `gsBackup` import and all references (`alarmListener` backup branch, `syncAlarmWithSettings` call in startup chain), `gsBackup.js` belongs to the session-backup feature, not this branch.
- `src/js/options.js`: moved `window.location.reload()` for the `LANGUAGE` preference to after `saveChange()` completes, preventing a race condition where the page could unload before the new locale value was written to storage.
- `README.md`: visual overhaul, centered header with `suspendy-guy-main.png` mascot, tagline, and four shields.io badges (license, Manifest v3, Chrome ≥ 110, Crowdin l10n); Marvellous Codeworks logo (`marvellous-codeworks-logo.png`) with brand attribution below the badges; `suspendy-guy-lotus.png` accent in the Shoutouts section; fixed broken markdown links (permissions bare URL, tab-recovery line break); removed BrowserStack shoutout (no longer a dependency); added Manifest V3 / Chrome 110 requirement callout to install-from-source; minor prose cleanup throughout.
- Branch rebased onto `master` (2026-06-15) to incorporate bugfixes from v8.1.4 (#369 Chrome tab group bug, #370 favicon cache, #371 logs and linter, #372 manifest prep). No conflicts with the visual-redesign changes.
- Branch rebased onto `master` (2026-06-24) to incorporate v8.1.5 (#374 Edge tab group bug fix, #376 tab recovery for non-suspended tabs). Our version stays at 8.2.0. Conflict on `manifest.json` (version) resolved keeping 8.2.0; `gsSession.js` and `background.js` absorbed master's Edge fix cleanly alongside our changes.
